# Project Structure — modular-hx-ts

**This file is the build queue and the session state.** A new session reads it,
finds the first unticked box, and builds that.

*A stale tree is worse than none — it is what tells us what to build next.*
Rule **R3** checks it against the filesystem.

Tick a box when the file compiles, its tests pass, and it has a note.
Mark files to hand-type with `[hand]` up front — per file, not per mood.

---

## Phase 0 — Rules before code

**Nothing else starts until every box here is ticked.** On an empty repo these
pass trivially and need no allowlist. That is the only moment that is true.

- [x] `Makefile` — dev · test · lint · ci · infra-up · migrate
- [x] project manifest + toolchain pin
- [x] `docker-compose.yml` — postgres, health-gated, non-default host ports
- [x] `.env.example`
- [x] arch test — S1 layering · S5–S9 boundaries · S10 vendor confinement
- [x] docs test — N1 notes · N3 sections · N5 cited paths exist · R3 tree matches
- [x] CI workflow — no-infra jobs first, then integration, then e2e
- [x] `docs/decisions/0001` written and referenced by the arch test (rule D5)

## Phase 1 — L0 Kernel

Pure. No I/O, no process state. Runs with no fixtures, no fakes, no infrastructure.

- [x] `src/shared/errors/`
- [x] `src/shared/result/`
- [x] `src/shared/brand/`
- [x] `src/shared/assert/`
- [x] `src/shared/clock/`
- [x] `src/shared/id/`
- [x] `src/shared/random/`
- [x] `src/shared/retry/`
- [x] `src/shared/breaker/`
- [x] `src/shared/digest/`
- [x] `src/shared/pagination/`
- [x] `src/shared/buildinfo/`
- [x] `src/shared/redact/`

## Phase 2 — L1 Runtime

This process: configuration, logging, lifecycle, observability.

**Landed early, deliberately:** `src/main.ts`. The composition root belongs to
phase 8, but a process that boots and stops is what lets each L1 module be
exercised for real as it is built rather than only through unit tests. It stays
a skeleton until phase 8, and every placeholder in it names the module that
replaces it — `env` for configuration, `logger` for output, `lifecycle` for
ordered shutdown, `httpx` for a server that actually listens.

- [x] `src/shared/provenance/`
- [x] `src/shared/logger/`
- [x] `src/shared/env/`
- [x] `src/shared/secrets/`
- [x] `src/shared/lifecycle/`
- [x] `src/shared/health/`
- [x] `src/shared/telemetry/`

## Phase 3 — L2 Substrate

**Each one: port + memory adapter + real adapter + one contract suite both pass.**
That is the layer's definition, not a convention inside it.

- [x] `src/shared/postgres/` — with `testx`; one unit of work
- [x] `src/shared/events/` — port + memory + outbox, one contract suite
- [x] `src/shared/jobs/` — with `lock`; one unit of work
- [x] `src/shared/lock/` — port + memory + postgres advisory
- [x] `src/shared/mailer/` — port + memory + smtp + none, one contract suite
- [x] `src/shared/httpclient/` — **L2 complete**

## Phase 4 — L3 Capability

Real decisions, no domain knowledge.
**`classification` before anything that reads it.**

- [x] `src/shared/authz/` — deny by default, Subject explicit
- [x] `src/shared/tenant/` — single mode byte-identical; the fence is not a grant
- [x] `src/shared/crypto/` — the keyring is the module
- [x] `src/shared/classification/` — **first in L3**, and before its consumers
- [x] `src/shared/flags/` — **L3 complete**

## Phase 5 — L4 Edge

- [x] `src/shared/edge/` — **the floor of L4**, and first for the reason `errors` is first in L0
- [x] `src/shared/httpx/` — the order is specified rather than chosen
- [x] `src/shared/idempotency/` — fills position 9; two clocks, and the key is scoped
- [x] `src/shared/ratelimit/` — fills position 7; shared state, and it degrades rather than switches off
- [x] `src/shared/conditional/` — the RFC half; the derivation half is an interface with no implementer
- [x] `src/shared/openapi/` — **generated, committed, diffed.** The parking reason expired when the third context landed: it was parked because with no handlers there was nothing to demonstrate the guarantee on, and there are now three contexts, 29 operations and `S11` enforcing every declared response set — so the document inherits that and is more honest than a hand-written one
- [x] `src/contexts/identity/` — users, sessions, roles, challenges, API keys
- [x] `src/contexts/audit/` — subscribes to every context; idempotent by event id
- [x] `src/contexts/orgs/` — multi-org membership, role **per organization**, one owner always. The last of the constant set

### Showcase

- [ ] `src/contexts/lineage/`
- [ ] `src/contexts/inbound/`
- [x] `src/contexts/webhooks/` — signed delivery with retries, and **the first caller `httpclient`, `breaker` and `retry` have ever had**
- [x] `src/contexts/exports/`

## Phase 8 — Composition root

- [x] `src/wire.ts` — the only place that knows concrete types
- [x] `policy` — the one authz policy, validated at boot
- [x] `config` — the storage switch
- [x] `migrations` — registry of every context's set
- [x] `seed` — the **bootstrap administrator**, from configuration
      (`../CONTEXTS.md` §7.4). Granting a role needs a role, so an empty
      database cannot reach one; `seed` mints the base case through the normal
      path, refuses when unset, and is idempotent by address
- [x] `seed --demo` — realistic demo data. **A blueprint that boots empty makes
      a bad first impression**, and rung 0a makes that an acceptance criterion:
      six people, a role granted, a suspension and reinstatement by the
      administrator, and every record with a real request behind it. `make dev`
      seeds itself, because in memory mode `make seed` would be a different
      process and a different empty map

## Phase 9 — Verification

- [x] `STORAGE=memory` runs the whole app with zero dependencies (conformance 47)
- [x] Integration suites green against real Postgres
- [x] e2e green against the real binary — `tests/e2e/`, both storage modes
- [ ] `../conformance` runner green — **23 / 1 / 23**, unchanged by `webhooks`,
      which the suite has no cases for. Measured on port 17471 with `/version`
      confirmed against this process first — §3.9. The one failure is
      `authenticate`: it wants `refresh_token`, the slice is not built, and line
      176 says failing is better than stubbing it
- [ ] `notes/INDEX.md` generated and current

---

## Later — deferred, with triggers

**A deferral without a trigger is an omission** (rule M11).

| Module | Trigger |
| --- | --- |
| `dlq` | The second thing that can dead-letter |
| `backlog` | Anything can fall behind |
| `cache` | A measured need, or a second instance where in-process is wrong. **Must be tenant-namespaced on arrival** — one tenant's cached user answering another's request — and the namespace belongs in the key builder rather than in every caller |
| `search` | Anything is queried by free text |
| `quota` | The product has plans, limits, or metered consumption |
| `attest` | This blueprint gains a data-provenance story |
| `deadline` | The first timeout that should have been a budget |
| `llm` · `prompts` · `tokens` · `evals` | The first model-backed feature |
| `oidc` · `saml` · `mfa` | The first enterprise buyer |
| `maintenance` | The first migration that cannot run online |
| `backfill` | The first schema change needing data moved |
| `scaffold` | The third bounded context |

## File descriptions

Filled in as files are created. **Rule R3 requires every source file to appear
here and every listed file to exist.**

### Root

| File | Description |
| --- | --- |
| `.gitignore` | Dependencies, build output, environment files, editor state, and local working files that are not part of the published blueprint. `.env.example` is committed; `.env` never is |
| `.github/workflows/ci.yml` | Four jobs, no-infra first so most failures land before a container starts: `make ci`, `make audit`, then integration and e2e against Postgres as a service container. Node and pnpm come from `.nvmrc` and `packageManager`, so CI cannot drift from a developer's machine. Same image and tag as compose; the tuning flags compose passes via `command:` are the one thing a service container cannot express, and the file says so |
| `Makefile` | The interface to the verification ladder. Rung 0 (`dev`, `test`) needs no infrastructure; rungs 1–3 (`migrate`, `test-integration`, `e2e`) need Postgres. `ci` is every check a push must pass without Docker. Host ports come from `../PORTS.md` (this repo's base is 15420) and are env-overridable. Compose targets cover Postgres only — Mailpit and Jaeger/Prometheus targets land with `mailer` and `telemetry` |
| `package.json` | Manifest. `type: module`, `packageManager` and `engines` pin the toolchain. Scripts are only the ones the Makefile calls — `make` is the interface, so there is one door per job. `dependencies` is empty on purpose: a runtime dependency arrives with the module that needs it. **Two TypeScripts on purpose**: `typescript7` (aliased 7.0.2, the native compiler) builds and typechecks, while `typescript` 6.0.3 is what `typescript-eslint` and `dependency-cruiser` read the AST with — typescript-eslint has no TS 7 support, so this is the side-by-side layout TS 7 ships for. `tsc` on `PATH` is therefore ambiguous; every script names the compiler by path instead |
| `pnpm-lock.yaml` | Resolved dependency graph. Committed; `make install` uses `--frozen-lockfile` |
| `pnpm-workspace.yaml` | pnpm's own settings (pnpm 11 reads them here). Postinstall scripts run only for dependencies named in `allowBuilds`; `minimumReleaseAgeExclude` records every version deliberately taken newer than the supply-chain gate |
| `.nvmrc` | Node pin, exact. Read by `nvm` and by `actions/setup-node` in CI, so local and CI cannot drift |
| `.npmrc` | `engine-strict` — the wrong Node fails at install, not three rungs up the ladder |
| `tsconfig.json` | Typechecking, no emit. Strict plus every flag above it that catches real bugs; `erasableSyntaxOnly` keeps the program identical for tsc, esbuild and native type stripping. Covers `src`, `tests` and the config files |
| `tsconfig.build.json` | Emit only, and test tooling is excluded so it cannot ship. `src` → `dist` as plain ESM that `node` runs with no loader and no flags — what rungs 3 and 4 actually execute |
| `vitest.config.ts` | The ladder as three projects: `unit` (no infrastructure, and where the rule suites live), `integration`, `e2e`. `make test`, `make test-integration` and `make e2e` select between them |
| `docker-compose.yml` | The local stack for rungs 1–3. Postgres and **Mailpit**, the two services a phase-1 module has a contract suite to run against; Mailpit arrived on the same change as `mailer`, which is `../INFRASTRUCTURE.md` §3 rule 7 working in the direction it was written for. Mailpit is health-gated like Postgres — its probe is its own binary, since the image ships no shell — and binds `../PORTS.md` +2 and +3. `postgres:18-alpine`, pinned by tag — note this diverges from `../INFRASTRUCTURE.md` §2.1, which still records `postgres:16-alpine` for the collection; the deviation is recorded in `docs/decisions/0002-postgres-18.md`. Postgres only — the one service a phase-1 module has a contract suite to run against; Mailpit arrives with `mailer`, Jaeger and Prometheus with `telemetry`. Image pinned by tag, health-gated so `--wait` means something, host port `15420` — `../PORTS.md` base 15420, Postgres at offset +0 — overridable via `PG_PORT`, named volume destroyed only by `db-reset`/`infra-reset`, mounted at `/var/lib/postgresql` because 18 moved the data into a version-named subdirectory below the mount. `--locale=C` so `ORDER BY` means the same thing here, in CI and on every machine — keyset pagination depends on it. `fsync=off` and friends are deliberate: rung 2's database is disposable, and that trade stays out of `deploy/` |
| `.env.example` | Every environment variable with its memory default, each labelled with the module that reads it. An empty `.env` is valid — the defaults run the whole application with nothing installed, which is acceptance criterion 1 as a file. The commented rung-2 block is the entire switch to real infrastructure, and it is config, not a code path |
| `.dependency-cruiser.cjs` | S1-S10 as import-graph rules, plus `no-circular`. `S3` covers two test-tooling suffixes — `.contract.ts` and `.testkit.ts` — because TypeScript has no package-private, so the boundary Go gets from a `_test` package has to be a rule here. Anchored on `src/` and cruised with a `baseDir`, so one rule set governs the real tree and the fixture trees alike. Its config schema forbids extra keys, which is why the layer table lives next door |
| `layers.cjs` | The layer of every shared module, and the vendor SDK table. One source of truth read by two independent enforcers: the S1/S10 rules above, and the docs test that checks each module note names the same layer (N7) |
| `eslint.config.ts` | Lint, type-aware from the start via `strictTypeChecked`. Deliberately holds no architecture rules — S rules are import-graph rules and belong to dependency-cruiser, M rules are AST rules and belong to `tests/rules/`. Each class of rule sits in the tool that can express it |
| `.prettierrc.json` · `.prettierignore` | Formatting, enforced by `make fmt-check` inside `make ci`. Markdown is excluded — prettier normalizes `*emphasis*` to `_emphasis_` and reflows ASCII tables, diverging this repo from the collection's documents |

### Shared kernel

| File | Description |
| --- | --- |
| `src/shared/errors/index.ts` | **L0.** Kind-tagged errors: a closed vocabulary of ten kinds, `wrap` that adds context per layer while preserving the kind, and `isRetryable` decided once so `retry` and `breaker` cannot disagree. Deliberately holds no status codes — invariant I7 maps kinds to transport codes only in transport, and this is the module where that erodes first. The only module a context's `domain/` may import (rule S7). Note: `notes/patterns/errors.md` |
| `src/shared/result/index.ts` | **L0.** `Result<T, E = AppError>` as a discriminated union with standalone helpers — no methods, so it survives `JSON.stringify` and crosses a process boundary. `attempt`/`attemptAsync` are the adapter boundary in one function: third-party code throws, this side returns classified values. Imports `errors` sideways within L0, which `S1` permits and `../ARCHITECTURE.md` §2 asks be flagged. Note: `notes/patterns/result.md` |
| `src/shared/result/index.test.ts` | 23 unit tests, including the JSON round-trip that justifies the union over a class, and both non-`Error` failure paths that real drivers produce |
| `src/shared/brand/index.ts` | **L0.** Nominal types on a structural type system. The tag is a `declare`d `unique symbol`, so a brand is erased entirely — a `UserId` **is** a `string` at runtime. `defineBrand(name, predicate)` makes the predicate the definition of the type, so a function taking a `UserId` needs no defensive check. Note: `notes/patterns/brand.md` |
| `src/shared/brand/index.test.ts` | 15 tests. The three that carry the module are type-level `expectTypeOf` assertions — the behaviour is invisible at runtime, so they are checked by `make typecheck` rather than by the test run |
| `src/shared/assert/index.ts` | **L0.** `invariant` · `assertDefined` · `must` · `assertNever` · `unreachable`. All throw `Internal`, which is the other half of the rule `result` encodes: a failure you expected is a value, one you did not is a throw. `assertNever` turns a new union member into a compile error rather than an accidental 500. Note: `notes/patterns/assert.md` |
| `src/shared/assert/index.test.ts` | 16 tests. Pins the deliberate asymmetry — `assertDefined` accepts `0`/`''`/`false` while `invariant` rejects them — and asserts that a structural value never reaches a message, secrets included |
| `src/shared/clock/index.ts` | **L0.** Time behind a port, per invariant I5 — the only file permitted to read `Date.now`, `new Date()` or `performance.now()`. Exactly two readings, `now()` and `elapsed()`, with the surface fixed by `../MODULES.md` rather than chosen locally; waiting is deliberately off the port, so a consumer declares its own. Exposes wall-clock and monotonic time separately, because subtracting two wall-clock readings computes a negative duration whenever NTP steps the host backwards. `fakeClock` only moves when a test moves it, so an hour of backoff costs a millisecond. `Millis` is branded so seconds cannot be passed where milliseconds are meant. Note: `notes/techniques/clock.md` |
| `src/shared/clock/index.test.ts` | 17 tests. The load-bearing one advances ten seconds, jumps the wall clock back six years, and asserts the monotonic reading still says ten seconds |
| `src/shared/id/index.ts` | **L0.** UUIDv7 behind a port, per invariant I5. Time-ordered so an id column is a usable sort key — a B-tree appends rather than fragmenting, and keyset pagination over it is chronological. Monotonic inside a millisecond via RFC 9562 §6.2 method 1, because a burst of inserts is exactly when ordering silently stops holding. Declares `RandomBytes` itself: `random` is not built yet, and the consumer owns the interface. Note: `notes/techniques/id.md` |
| `src/shared/id/index.test.ts` | 21 tests. 500 ids inside one frozen millisecond sort correctly and are distinct; a golden encoding value pins the RFC byte layout; the 4096-per-millisecond counter wrap is a tested boundary rather than a surprise |
| `src/shared/random/index.ts` | **L0.** The one CSPRNG, per invariant I5 — so `Math.random()` anywhere else reads as an anomaly rather than an idiom. `int` uses rejection sampling because `draw % max` skews toward low residues. `constantTimeEqual` wraps `node:crypto`'s `timingSafeEqual` rather than hand-rolling the loop, since a hand-written accumulator is correct on paper and at the mercy of the JIT. Note: `notes/techniques/random.md` |
| `src/shared/random/index.test.ts` | 20 tests. Uniformity is measured over 60,000 draws rather than asserted; the 200,000-byte draw proves chunking past the platform's 65,536 single-call limit; `constantTimeEqual` is checked against `===` on every case, including ones differing only in the first or last byte |
| `src/shared/retry/index.ts` | **L0.** Kind-aware backoff with full jitter. Repeats only failures `errors.isRetryable` calls transient, so `retry` and `breaker` cannot drift apart. `random(0, min(cap, base × 2^(n-1)))` — full rather than equal jitter, because equal jitter's floor keeps every client inside the same window and the herd re-forms. Clock and randomness injected, so an hour of backoff is verified in a millisecond. Note: `notes/techniques/retry.md` |
| `src/shared/retry/index.test.ts` | 19 tests. Jitter spread and decorrelation are measured rather than asserted; a 24-hour simulated backoff completes in under two seconds of real time; overflow is pinned at attempt 5,000 |
| `src/shared/breaker/index.ts` | **L0.** Keyed circuit breaker over a rolling window, with one half-open probe. The window is the point: a consecutive-failure counter resets on every success, so a dependency failing half its calls never trips — and that flapping case is the one that fills a connection pool. Counts only failures `errors.isRetryable` calls transient, so `retry` and `breaker` share one definition. `onStateChange` satisfies invariant I9's requirement that the control be visible when it fires. Note: `notes/techniques/breaker.md` |
| `src/shared/breaker/index.test.ts` | 16 tests. The load-bearing one alternates failure and success and asserts the circuit opens — a consecutive counter would sit at one forever |
| `src/shared/digest/index.ts` | **L0.** Canonical JSON per RFC 8785 and `sha256:` content identities. `JSON.stringify` preserves insertion order, so two objects with the same fields hash differently depending on how they were built — that single fact is why this exists. Refuses `undefined`, `NaN`, `Date`, `Map` and cycles rather than guessing, because each guess is a divergence that shows up as a mismatched identity long after the cause. Note: `notes/techniques/digest.md` |
| `src/shared/token/index.ts` | **Mint a secret, store only its shadow.** Promoted out of `identity/app/tokens.ts` when `orgs` needed the identical mechanics for an invitation — `S6` makes a context's code unreachable from the next one, and the **second copy** is the collection's trigger. The half that could *not* be promoted is the more interesting one: `CONTEXTS.md` §4 calls an invitation *the `Challenge` shape again*, and the aggregate cannot be shared, because `S7` permits `domain/` exactly one import. That is the rule working — an aggregate is a context's own model, and two contexts sharing one would be two contexts with one model. The shape is reused; the model is not |
| `src/shared/secretlink/index.ts` | **The wire half of an out-of-band secret**: `<id>.<secret>.<tag>`, issued once and emailed. Separate from `token` because a session has no MAC, no identifier on the wire and no journey outside the system — one module doing both would make every session carry link machinery it never uses, and the layer boundary makes that structural, since a module at L0 cannot have a MAC in it. **The wire tag binds the id and nothing else**, so a forged identifier is refused by arithmetic before the database is touched; a second tag over the *contents* stays with the aggregate, because it binds domain values. No clock, no TTL, no purpose — those are rules, and `S7` puts no shared module within reach of the aggregate that owns them |
| `src/shared/secretlink/index.test.ts` | 19 cases. Two found real defects on first run: a tag contains dots (`v1.<kid>.<tag>`), so an exact three-way split refused every link this module issues; and the module exported a refusal, which made *malformed* say something different from *expired* — the case-13 oracle, introduced by the extraction and caught by the test asserting the two are equal. One case reads this module's own source to assert there is no clock in it |
| `src/shared/digest/index.test.ts` | 34 tests. Reproduces RFC 8785's published worked example byte for byte, and pins one digest against a hash that `shasum` and `openssl` both independently confirm. The last 15 are **not ours**: the collection's cross-language vectors from `../conformance/fixtures/canonical-json.json`, verified across Python, Go and TypeScript before being committed and asserted here as hex **and** digest. They exist because a suite written beside its implementation cannot detect that both are wrong in the same way — which is how the collection lost `err_kind` in five repositories at once, each with a green suite. Read in place rather than copied, and **skipped by name** in a checkout without the sibling directory; they vendor into `docs/` with publication, alongside the root documents |
| `src/shared/pagination/index.ts` | **L0.** Keyset cursors. `OFFSET` is O(offset) *and* incorrect under concurrent writes — an insert before the offset makes the reader skip a row, a delete makes it see one twice, and the symptom is a missing record nobody knew to look for. A cursor names a position in the data, so concurrent writes cannot shift it. Carries its ordering, so one listing's cursor cannot be replayed against another. Note: `notes/techniques/pagination.md` |
| `src/shared/pagination/index.test.ts` | 20 tests. Walks 25 rows through 3 pages and asserts every row appears exactly once and in order; pins the exactly-full-page and cursor-position off-by-ones that would each skip or repeat a row per boundary |
| `src/shared/buildinfo/index.ts` | **L0.** Name, version, commit, build time, dirty flag — the answer to "what is actually deployed", in the startup line, at `/version`, and in every outbound `User-Agent`. Reads no environment: `process.env` is L1's concern, so the root passes values in, which is also what makes the formatting testable without a build. Fails open per invariant I9 — a bad stamp degrades to `unknown` rather than stopping a boot during a rollback. Note: `notes/patterns/buildinfo.md` |
| `src/shared/buildinfo/index.test.ts` | 17 tests. Seven shapes of garbage input assert it never throws, and an unsubstituted `$COMMIT` becomes `unknown` rather than being echoed as if it were a sha |
| `src/shared/redact/index.ts` | **L0.** One place for "this must never print" — the `×5` harvest module. A `Secret` is unprintable by construction across all four paths from a value to text: `Symbol.toPrimitive`, `toString`, `toJSON`, and `nodejs.util.inspect.custom`, with the value in a `#private` field. `util.inspect` is the one most implementations miss, and it ignores `toString` entirely. `expose()` is deliberately ugly so every call site is greppable. Note: `notes/patterns/redact.md` |
| `src/shared/redact/index.test.ts` | 31 tests, one per leak path. Found three real defects: hyphenated keys like `X-Api-Key` slipped the fragment list; `Secret` declared its stringification only on the class, so the type system could not see the guarantee; and a fragment of three characters matched as a substring, so `pan` redacted `span` in a real telemetry log line. Short fragments now match a whole segment — `card_pan` still redacts, `span` and `panel` do not |
| `src/main.ts` | **L5 composition root**, landed early (see phase 2). `migrate` runs the real migrator against `DATABASE_URL` over `ALL_MIGRATIONS`, which `wire.ts` assembles — it was `[]` here for six phases and nothing noticed, because the integration suites each build their own schema and never ask this list what is in it. `serve` now mounts the wired handler on a real server, registers the outbox relay as a component ahead of it so reverse-order shutdown drains what the last request produced, and announces every slot the root left unwired. Exits `78` without a DSN, which is what keeps the smoke test hermetic. Runs every command inside an origin — `system:boot`, `system:migrate` — so a line emitted by code that never asked for provenance still carries it. The only place that knows concrete types and the only place that reads the environment; rule `S9` keeps anything outside the root from importing it — `main.ts` → `wire.ts` is the root importing itself, which `S9` had forbidden and which would have made a one-file composition root mandatory. Five commands — `serve`, `version`, `secrets`, `migrate`, `doctor` — with shutdown owned by `lifecycle` rather than hand-rolled — `health` registered last so draining is the first thing shutdown does — and with configuration loaded once through `env` — secret references resolved first by `secrets` — and a bad environment exiting `78` (`EX_CONFIG`) after naming every problem from both modules together. `version` and `secrets` deliberately need no configuration — the moment somebody asks what is deployed is usually the moment it is broken, and a broken reference is *why* configuration will not load, so the command that diagnoses it cannot depend on it. so `make dev`, `make start` and `make migrate` all do something real. `doctor` exercises every wired dependency and reports what happened, which is the operational answer to "is this deploy wired correctly"; it is not a health check, and `health` (L1) brings the probes a running process needs. Exports `main` and `wireKernel` and boots only when executed directly, which is what lets the smoke test import it |
| `src/wire.ts` | **The assembly.** `ARCHITECTURE.md` §3 rule 5: a context is reached only through its root and only by this file, so it is the one module that may see `identity` and `audit` at once. Compiles the one policy at boot — `compilePolicy` returns a `Result`, so a policy naming an action nobody defined fails the process rather than becoming a 403 somebody debugs in production — and names the roles a signup confers, because *which* roles is a product decision belonging to neither context. Lends `audit` identity's bearer auth as an authz `Subject` (`CONTEXTS.md` §3), which is what keeps `S6` true while still giving it an authenticated principal. Its `skipped` list is a claim, not an absence: an empty one says nothing was left out. Position 9's exempt set is **read off the route table** rather than listed by hand — `auth` is declared per route, so the answer already exists, and the hand-kept first version was already wrong |
| `src/demo.ts` | **Rung 0a's other half** — `../INFRASTRUCTURE.md`. The bootstrap administrator makes roles reachable; this makes the system look *used*: six people with real names, a role granted, everybody logging in and out, and one account suspended and reinstated by the administrator so the trail has an entry where the actor is not the subject. **Driven through the HTTP surface, never the repositories** — the same constraint the conformance corpus loader carries, because a world that cannot be created through the API describes a state the product cannot reach. Idempotent: a 409 is the second run, not a failure |
| `tools/curl.ts` | `make curl`. **Generated, never written** — hand-maintained example requests drift within a week and then mislead, which is worse than none. It reads the **route table the server mounts**, so a path that moves moves here and a route that does not exist cannot be printed. Two bugs found by running it rather than reading it: `$RANDOM` inside a single-quoted `-d` is four literal characters, and `-H 'if-match: $ETAG'` sends the five characters `$ETAG` — the second answered 412 forever while looking exactly right in the transcript. A generator carrying the bug it exists to prevent is why this is executed end to end in the stranger walkthrough |
| `tools/routes.ts` | `make routes` — every route served, with its authentication. Read off the same tables `wire` mounts, so it cannot drift from what is actually answered; a hand-written inventory is a different document that happens to look like one. Five blueprints put their surfaces side by side and disagreed on everything except six routes, which is what `../CONFORMANCE.md` §3.5 now settles — and comparing them needs the list rather than a count |
| `tools/statuses.ts` | `make statuses LOG=<access log>` — which declared statuses this repository has ever been **observed** to produce. `S11` proves a route declares every status its chain can produce; it does not prove the converse, and `../ENFORCEMENT.md` now says so in a sentence this repository supplied the example for — `PATCH /v1/users/{id}` declared `428` with a comment explaining why, and answered `400` for six phases, green throughout. It **reports and never fails**: a status reachable only when a dependency is down is legitimate, and a rule that failed on one would be suppressed within a week. Reads the access log offline and matches paths back to templates, so nothing in `src/` learns it is measured — the numbers come from whatever actually ran, including the conformance runner against a real binary. It matters because `openapi` publishes the declaration either way, and an unproduced status is documentation of behaviour that does not exist |
| `src/shared/errors/index.test.ts` | 26 unit tests. Four exist to pin decisions that would otherwise erode: a conflict is not retryable, an absent `cause` is not `cause: undefined`, `wrap` survives a thrown non-Error, and `chain` terminates on a cycle |

### Runtime

| File | Description |
| --- | --- |
| `src/shared/env/index.ts` | **L1.** Typed configuration with no dependency. Components declare readers for what they own; nothing keeps a central list of every variable. Note: `notes/patterns/env.md` |
| `src/shared/env/source.ts` | Where values come from — a one-method port, so a test never mutates `process.env`. It is also the seam `secrets` wraps: `../ARCHITECTURE.md` §8 resolves `file://` and `env://` references **before** parsing, and `env` never learns it happened |
| `src/shared/env/readers.ts` | `text` · `integer` · `flag` · `oneOf` · `url` · `duration` · `sensitive` · `optional`. Refuses `8080abc` rather than truncating it, and `FEATURE=treu` rather than reading it as false. `sensitive` returns a `Secret` and has no fallback, because a default credential is useless or dangerous |
| `src/shared/env/load.ts` | Schema plus source to typed config — collecting **every** problem, so a broken deploy is fixed in one pass rather than one variable per restart. `explain` renders it for stderr, since the logger's own level comes from here and does not exist yet |
| `src/shared/env/index.test.ts` | 24 tests. The load-bearing one asserts four problems reported together; type-level assertions pin that the schema's literal unions, `Millis` and `Secret<string>` all survive inference |
| `src/shared/secrets/index.ts` | **L1.** Secret references resolved before configuration parses, per `../ARCHITECTURE.md` §8. Works by wrapping `env`'s `Source`, which is why that port exists — `env` never learns anything happened and no schema changes. Note: `notes/patterns/secrets.md` |
| `src/shared/secrets/reference.ts` | What a reference looks like: `file://` and `env://`, and nothing else. A value that merely contains `file://` is a literal, because a password may begin with almost anything. Carries the one escape `../MODULES.md` §2 requires — a `literal:` prefix is stripped and the remainder returned **verbatim**, so the reference syntax cannot make a password that genuinely begins `env://` unrepresentable. Not trimmed, unlike a reference: a reference with surrounding whitespace is a typo, a password with a trailing space is a password |
| `src/shared/secrets/filesystem.ts` | The two filesystem calls this needs, injectable so a test never touches a disk. Reads are synchronous because configuration is parsed before the process does anything, and capped at 1 MiB because a secret is a credential, not a database |
| `src/shared/secrets/resolve.ts` | The resolver. A Kubernetes mount is a directory of one file per key, so the directory form is tried first — which is what makes `../INFRASTRUCTURE.md` §7.1's "no new code" true. Problems are collected rather than thrown, so a broken reference joins the rest of the configuration report |
| `src/shared/secrets/inspect.ts` | The check command's engine — `../MODULES.md` §2: *prints each reference, its source, and a will-it-boot exit code, without printing a value.* Replaces the restart loop, where a broken reference surfaces as a process exiting 78 with one line, then again with the next, one variable per restart against a deployment already down. Resolves **through** `resolving` rather than beside it, because a check with its own copy of the resolution path diagnoses a different program and agrees with boot until the moment it matters. Rendering lives here, not in the composition root, because the no-value guarantee is this module's to keep |
| `src/shared/secrets/index.test.ts` | 34 tests. Covers the k8s mount shape, `key=value` and JSON selection, a reference loop, that a failure message never carries the file's contents, the `literal:` escape in a chain as well as at the top, and that the check command prints no value while naming every broken reference at once |
| `src/shared/health/index.ts` | **L1.** Liveness and readiness, deliberately kept apart. Liveness runs **no checks** — if it touched the database, a database blip would restart every pod at once and a bad minute would become a bad hour (conformance 41). A failing `optional` check is **degraded, never down**, because failing readiness on a backlog hands traffic to the instances already behind on the same queue (conformance 42, `../INFRASTRUCTURE.md` §7.4). `drain()` refuses traffic while the process stays assembled. Note: `notes/patterns/health.md` |
| `src/shared/health/index.test.ts` | 20 tests. Pins that liveness runs nothing even when every dependency is down, that an optional failure still serves traffic, and that a hung check fails on a deadline rather than hanging the probe |
| `src/shared/telemetry/index.ts` | **L1.** Traces and metrics behind a port, with two adapters that need no dependency: `noopTelemetry` and `memoryTelemetry`. The OTel SDK is confined to this module — rule `S10`, and the worked example `../ENFORCEMENT.md` uses for it — because an SDK that spreads *becomes* the interface and replacing it stops being a decision anybody can make. Correlation is read from the ambient provenance and never written back: `logger` emits `trace_id` from the same `traceparent` without importing this module, so a log line does not depend on whether tracing was configured. `inSpan` ends a span whatever happens, and a failed one keeps the error's `Kind` — the same promotion `err_kind` makes in the log record. **Deferred with a trigger:** the `otlp` and `prometheus` exporters, and the Jaeger and Prometheus services in compose, land together with the first module worth tracing — `postgres` or `httpx` — because a service nothing tests against is dead weight (`../INFRASTRUCTURE.md` §3 rule 7) and a tracer with nothing to trace proves nothing. Selecting one today is reported as unwired rather than silently dropped. That is also when this repo takes its first runtime dependency. Note: `notes/patterns/telemetry.md` |
| `src/shared/telemetry/index.test.ts` | 18 tests. Pins that the noop still returns and still throws, that ambient correlation cannot be forged by a passed attribute, that a span survives a throw and keeps its `Kind`, and that `open()` catches a leaked span |
| `src/shared/lifecycle/index.ts` | **L1.** Ordered start, reverse-order stop, signals. Reverse order holds after a failed start too — what started is stopped, what never started is not, because a half-started process holds ports and locks nothing will release. A component that will not stop is abandoned on a deadline: a process that refuses to exit is `SIGKILL`ed and loses what the others were about to finish. Declares its own `Reporter` rather than importing `logger`, so the shutdown path depends sideways on nothing. Note: `notes/patterns/lifecycle.md` |
| `src/shared/lifecycle/index.test.ts` | 23 tests. Ordering, the unwind after a failed start, a hung component being abandoned while the rest still stop, and that `handleSignals` keeps the event loop alive — the bug that exits a process with code 13 before any signal arrives |
| `src/shared/logger/index.ts` | **L1.** Three implementations of one port — console, JSON, memory. The baseline has **no dependencies**: `consoleLogger` formats and colourises by hand. If a library ever earns its place it becomes a fourth implementation behind the same port and rule `S10` confines it here. `detectColour()` lives here until `env` lands, and nothing in the module calls it — the composition root does. Note: `notes/patterns/logger.md` |
| `src/shared/logger/logger.ts` | Level filtering, bound fields, ambient provenance and redaction — once, so the three sinks cannot disagree about what was logged. The one place in the repository that deliberately swallows an error: a closed pipe must not become an outage |
| `src/shared/logger/format.ts` | Console and JSON rendering, and nothing that can throw. `JSON.stringify` throws on a cycle, so it is wrapped; a log call becoming an exception while something else is already wrong is worse than a lossy line |
| `src/shared/logger/record.ts` | `Level`, `Fields`, `LogRecord` — in the shape `../MODULES.md` §2 specifies, with the field names normative because conformance case 54 checks them byte-identically across every blueprint. Separating the record from its rendering is what lets a test assert on fields rather than on a formatted string |
| `src/shared/logger/logger.contract.ts` | The one suite every adapter passes (rule `M1`). Asserts a **field set**, not bytes: console renders for a human and JSON for a collector, so they differ in format and never in fields. Four cases — plain, with an error, with full provenance, with none |
| `src/shared/logger/adapters.test.ts` | Runs that suite against all three adapters. 12 assertions that would pass happily as three near-identical suites and cannot as one |
| `src/shared/logger/index.test.ts` | 35 tests. Includes the regression for redaction dismantling errors, a sink that throws, and a value that cannot be serialized |
| `src/shared/provenance/index.ts` | **L1.** The module's only importable surface. Deliberately does not re-export `createProvenance`, the id shape predicates, or the testkit — rule `S2` makes reaching past this file a violation, which is how TypeScript gets the boundary Go gets from an unexported identifier |
| `src/shared/provenance/actor.ts` | Who is responsible: a closed four-value `Kind` with the vocabulary in the id path, so `system:reindex` changes no type and no canonical form. `onBehalfOf` is defined now and populated in phase 2 — adding it after the first signature would change every actor already signed |
| `src/shared/provenance/provenance.ts` | The record. Private fields and closed construction, because `PROVENANCE.md` §6 and §7 require custom marshaling anyway — which makes immutability free. `derive(causedBy?)` is the only way to get a child |
| `src/shared/provenance/origins.ts` | The five origins that mint, and the adoption boundary. `InboundHeaders` omits `requestId`, `actor` and `tenant` entirely, so §5's rule is enforced by the type rather than noticed in review |
| `src/shared/provenance/carrier.ts` | `AsyncLocalStorage`. `current()` never throws because a log line must never crash; `require()` raises `Internal` because a stamp point without provenance is a bug |
| `src/shared/provenance/ids.ts` | The normative id shapes — §5's charset and the W3C `traceparent` format. Internal: one definition, because divergence here is a spec violation rather than a style difference |
| `src/shared/provenance/actor.test.ts` | 17 tests. Pins the exact serialized bytes, including that `on_behalf_of` is omitted rather than `null`, and that a plain object cannot counterfeit an actor |
| `src/shared/provenance/provenance.test.ts` | 22 tests. Covers the conformance case `PROVENANCE.md` §10 says nothing currently covers — the same logical record canonicalizing to identical bytes with `causation_id` and `tenant` absent — plus a golden canonical string |
| `src/shared/provenance/origins.test.ts` | 16 tests. Seven hostile correlation ids are each dropped rather than failing the request, and `traceparent` is checked against its own W3C shape rather than a charset |
| `src/shared/provenance/carrier.test.ts` | 13 tests. Three concurrent requests with deliberately interleaved completion each see only their own provenance — if that leaked, every audit record would be suspect and only under load |
| `src/shared/provenance/provenance.testkit.ts` | The builder. Test tooling: rule `S3` keeps it out of shipping code and `tsconfig.build.json` keeps it out of `dist` |

### Substrate

| File | Description |
| --- | --- |
| `src/shared/postgres/index.ts` | **L2, and the layer's one exception.** Not a port with two implementations — it is what the real implementations are built on, and its memory counterpart does not exist because a memory repository does not use it. `M1` applies to the repositories above it, never here; there is **no fake pool**. Exports `DB` and `withinTx` and deliberately **not** `Transactor`, which is consumer-declared in `app/`: an application layer importing `postgres` to say "these writes are atomic" would have inverted nothing. Note: `notes/patterns/postgres.md` |
| `src/shared/postgres/db.ts` | `DB` — `query`, `queryRow`, `exec`, and **both a pool and a transaction satisfy it**. That dual satisfaction is what makes `withinTx` transparent, so a repository works identically inside a transaction and outside one with no second set of methods. Names SQL and parameters, never a query builder: a repository naming `Kysely` in its signature would have to change to swap it, which is `../MODULES.md` §3's test for the interface being in the wrong place |
| `src/shared/postgres/config.ts` | A **struct**, not an interface — nothing swaps it. The **DSN is a parameter**, which is structural rather than additive: a test appends `search_path=<schema>` to get its own, and a module reading the DSN internally would put every test on one schema. All three guardrails resolve here and ride on the DSN as libpq startup options, **merged** with whatever it already carries so `testx`'s `search_path` is not clobbered |
| `src/shared/postgres/pool.ts` | The concrete pool, and the transaction. Declares the `Transactor` **shape** for documentation and **deliberately does not re-export it** — it is consumer-declared in `app/`, and a use case importing `postgres` to say "these writes are atomic" has inverted nothing. What it does carry is `PoolSatisfiesTransactor`, a compile-time assertion: a consumer-declared interface cannot notice the provider drifting, so this breaks the build **in `postgres`** instead of later in a consumer holding a copy of the old shape. Both go through one `dbOver` so SQLSTATE translation cannot be forgotten in one of them. Holds two fixes found by the guardrail tests: a client that dies mid-transaction emits `error` on *itself*, and an unhandled one takes the process down; and a dying connection emits **twice**, so the first error is kept because the second is its own consequence |
| `src/shared/postgres/sqlstate.ts` | SQLSTATE → `Kind`, one of the four behaviours the storage suite pins. Deliberately not exhaustive: an unrecognised code is `Internal`, because a code nobody has considered is a bug in the query rather than something a caller can act on. `40001` and `40P01` are `Unavailable` rather than `Conflict` so `isRetryable` says yes — on the two failures that most want retrying |
| `src/shared/postgres/migrate.ts` | Forward-only, checksummed, namespaced per context, applied under a transaction-scoped advisory lock so N instances deploying together serialise rather than race. **Takes a pool and a set**, so a test can migrate into its own schema. Exempt from `statement_timeout` via `SET LOCAL` and **not** from `lock_timeout`: a migration may take as long as it needs, and must not hold the deploy open blocking live traffic |
| `src/shared/postgres/storage.contract.ts` | The storage-behaviour suite — **not** the repository contract suite. Asserts the substrate behaves the same regardless of how the SQL was produced: NULL ordering, SQLSTATE → `Kind`, isolation default and implicit transactions, timestamp round-trip. Run with one adapter because the properties are worth pinning when nothing is being compared, and written before the first repository exists, which is the only cheap moment |
| `src/shared/postgres/index.test.ts` | 17 tests, no database. The SQLSTATE table, the guardrail defaults, and that the DSN merge preserves an existing `options` — the case that would otherwise put every test on the default schema while appearing to give it one of its own |
| `tests/testx/postgres.ts` | The integration-test harness. **Not a peer module** (`../MODULES.md` §3), so it lives under `tests/` where `S3` needs no arguing and it never ships. Schema per test, which is what the DSN-as-parameter rule buys: each caller gets a fresh schema and a pool pointed at it, so suites run in parallel against one database. Adding a second service — Redis with `cache`, SMTP with `mailer` — is a file beside this one, not a redesign |
| `src/shared/events/index.ts` | **L2.** A port with **swappable providers**; the outbox is one provider, not the module. `EVENTS_PROVIDER=memory\|outbox`, exactly as `STORAGE=memory\|postgres`. The two providers **do not make the same promise** and that is not a defect in the port — `eventstest` asserts what they share and the note carries the difference. Note: `notes/patterns/events.md` |
| `src/shared/events/event.ts` | `<context>.<entity>.<verb>`, lowercase, exactly three segments — two is ambiguous about which half is the context and four invites a hierarchy nothing consumes. Payloads are **primitives only**: a payload crosses a process boundary, outlives the request in an audit record, and is read by code compiled against a different version, where a `Date` serializes three ways and a class instance serializes to `{}` |
| `src/shared/events/envelope.ts` | Where rule `M5` lives. `Envelope.seal` takes `Provenance` as a **required parameter**, because `M5`'s detect clause is *publish goes through the envelope constructor, which requires them* — checkable, where *"hopefully the context had it"* is not. Also `provenanceFor(env)`, the subscriber rule in one line: correlation from the envelope, causation from the **event id**, a fresh request id. It lives here rather than in `provenance` because `provenance` is L1 and `S1` forbids it importing an envelope, permanently |
| `src/shared/events/ports.ts` | `Publisher` · `Subscriber` · `Dispatcher`. `publish` takes the caller's `DB`, which is the whole point of the outbox and which the memory bus ignores — honest rather than sloppy, since it has nothing to make atomic. `Handler` allows a synchronous subscriber: appending to a projection should not have to wear `async` to satisfy a port |
| `src/shared/events/memory.ts` | The in-process bus, and **not a testing convenience** — it is what makes invariant `I1` possible, because an outbox needs Postgres and `make dev` needs nothing. Dedupes in-process because the contract requires dedupe of *every* provider; that record is lost on restart, which is consistent with the rest of its promise and is exactly the durability gap the outbox closes |
| `src/shared/events/outbox/index.ts` | The durable provider. Writes the event row **inside the caller's transaction**, so publishing is atomic with the data write. The relay claims with `for update skip locked` so N relays never coordinate and never wait, backs off with full jitter, and **dead-letters rather than drops** — an event nobody can handle is still evidence that it happened. Claim and dispatch are separate transactions, so an arbitrary handler never runs inside the database's idle-in-transaction budget |
| `src/shared/events/outbox/schema.ts` | The tables, namespaced `events`. **Two clocks, deliberately apart**: `next_attempt_at` is backoff, `lease_until` is ownership, and collapsing them is the bug that makes a slow consumer look like a dead one. The claimable index is **not** partial — the obvious predicate uses `now()`, which PostgreSQL refuses in an index predicate because it is `STABLE` rather than `IMMUTABLE` (`42P17`), caught the first time this ran against a real database |
| `src/shared/events/eventstest.ts` | One contract suite, **14 cases, run twice** — separate tests prove both providers work, one suite run twice proves they **agree**, and only the second is what `M1` asks for. Covers fan-out to every matching subscriber and to none, at-least-once via a **deliberate** redelivery, **dedupe not suppressing a legitimate retry**, containment of both a rejected promise and a thrown non-Error, the constructor refusing to publish without provenance (`M5`), and a subscriber **deriving** rather than minting (case 38). Deliberately does **not** assert durability across process death, because one provider does not have it |
| `src/shared/events/index.test.ts` | 20 tests. Event-name shape, primitives-only payloads, the `M5` constructor refusing to build without provenance, `fromWire` refusing bytes it cannot read, and the whole contract suite against the memory provider |
| `tests/integration/events/outbox.test.ts` | 20 tests. The same 14-case contract suite against the outbox — a **real** PostgreSQL through `testx`, per-test schema and real migrations, because the outbox's central claim is that the row is written in the caller's transaction and that is only testable against Postgres. Plus what only it has: a rollback taking the event with it, leasing so a second relay skips the row, backoff and ownership in separate columns, dead-lettering, and the dedupe row keyed per subscriber |
| `tests/rules/fixtures/semantic/m6-event-names-wrong-context/src/contexts/identity/domain/events.ts` | Trips `M6` — `identity` publishing under the `orgs` prefix. The rule lands with **no contexts in the repo**, which is the phase-0 principle: the rule arrives before the code it governs, so it never needs an allowlist |
| `src/shared/lock/index.ts` | **L2.** A named distributed mutex behind a port. Advisory locks are the adapter for one property above all: **a crashed holder releases automatically**, because the lock lives on the connection. No sweeper, no TTL that is a guess about how long a healthy holder pauses, and no window in which a dead instance still owns the fleet's singleton. Note: `notes/patterns/lock.md` |
| `src/shared/lock/port.ts` | `tryAcquire` · `withLock` · `releaseAll`. **Never waits** — a lock that queues turns a contended period into a pile of instances each holding a connection open, which is how a fleet-wide singleton becomes a fleet-wide outage. The instance that lost should do nothing and try again next period |
| `src/shared/lock/key.ts` | An advisory key is a **signed 64-bit integer**, so every name is hashed to one — and that space is shared across the whole database, where a collision blocks with no error and no log line. `int64(first 8 bytes of sha256(namespace + ":" + name))`, documented because it must not be re-derived by guesswork, and namespaced so `jobs:purge` and `leases:purge` cannot collide |
| `src/shared/lock/memory.ts` | The `STORAGE=memory` adapter. Correct within one process and **structurally unable** to demonstrate the survives-holder-death property, because there is no other process to lose. Not a defect — `STORAGE=memory` is one process by definition (`I1`) — but the reason the contract suite stops where it does |
| `src/shared/lock/postgres.ts` | **Session-scoped** `pg_try_advisory_lock`, not the transaction-scoped variant the migrator uses: a job running for a minute would otherwise lose its lock at the first commit. Every lease holds **its own connection** for its whole life, since releasing from a different pooled connection is a silent no-op. That requirement is why `postgres` grew `Session` — naming a `PoolClient` here would have meant importing `pg`, which `S10` forbids, and the rule pushed the concept into the right module |
| `src/shared/lock/locktest.ts` | One contract suite, both adapters. Mutual exclusion, hand-over on release, non-reentrancy, `withLock` releasing when the work throws, and `releaseAll`. **Deliberately does not assert survives-holder-death** — the memory adapter cannot express it, and a suite that "proved" crash-safety against an in-process `Set` would give exactly the confidence that must not be given |
| `src/shared/lock/index.test.ts` | 14 tests. The key derivation staying inside the signed 64-bit range, being stable (changing it would split a rolling deploy so old and new instances take *different* locks and both run), separating namespaces — plus the whole contract against memory |
| `src/shared/jobs/index.ts` | **L2.** Periodic maintenance: a declared job with an `area.verb` name, a period with jitter, a timeout and a singleton flag, run by a scheduler that supplies minted provenance, failure containment, a span and uniform logging from a returned count. Note: `notes/patterns/jobs.md` |
| `src/shared/jobs/job.ts` | The declaration, validated at registration — which is boot rather than 3am. `singleton` **defaults to `false` explicitly**: a cache sweep is local and every instance should do its own, and an implicit default is how a destructive job runs N times because nobody wrote the flag |
| `src/shared/jobs/scheduler.ts` | Provenance is **minted, never derived** — a job is the root of its own chain (`../PROVENANCE.md` §4, the mint row), and getting it wrong is quiet: the job runs, the records are written, and nothing joins to anything. Overlap is **refused rather than queued**, because an overlapping purge is two workers deleting the same rows and waiting turns one slow run into a queue that fires at once when it clears. Jitter spreads the fleet; the singleton lock makes the outcome correct either way |
| `src/shared/jobs/index.test.ts` | 15 tests. The mint row asserted field by field, containment of a throwing job, the timeout signalling rather than claiming the work stopped, overlap being skipped, and `list`/`runNow` — the operator door that exists because whoever cannot invoke a purge on demand will invoke it with SQL instead |
| `tests/integration/lock/postgres.test.ts` | 12 tests. The shared contract against a real database, plus the property only this adapter has: a lock **released when its holding backend is terminated**, verified by finding the holder in `pg_locks` and calling `pg_terminate_backend`. Also that twenty refused acquires do not leak twenty connections |
| `tests/integration/jobs/singleton.test.ts` | **The closing condition for both modules.** Two schedulers, concurrent, against one real database, and the job body executed **once** — the claim that lets this architecture deploy as one Deployment with N replicas and no separate cron process. Verified failable by making the flag a no-op. Plus: the lock released when the job throws, non-singletons running everywhere by design, and `stop()` freeing the fleet lock so a rolling deploy does not strand it |
| `src/shared/mailer/index.ts` | **L2.** `send(message) -> Receipt` behind a port with three adapters — `memory`, `smtp`, `none` — and one contract suite all three pass. Knows nothing about users, tokens or challenges: the decision to send belongs to a context, and to an **event subscriber rather than a command's transaction**, because a slow SMTP server must not hold a transaction open and a rolled-back registration must not already have sent a welcome email. Note: `notes/patterns/mailer.md` |
| `src/shared/mailer/message.ts` | **The security-relevant part.** SMTP headers are CRLF-separated, so a control character in a display name or subject does not corrupt a header — it adds one: a silent `Bcc`, a forged `From`. Every header-bound field is **rejected**, never sanitised, because a stripped newline turns an attack into an odd display name nobody investigates. The error names the field and never echoes the value, since a rejected header ends up in a log |
| `src/shared/mailer/port.ts` | `send` and nothing else. Deliberately inconvenient to call mid-write — no `send(userId, template)` convenience and no transaction parameter — so the temptation does not arise when `identity` lands |
| `src/shared/mailer/templates.ts` | `<name>[.<lang>].{subject,txt,html}`, **compiled at boot** so a malformed placeholder fails startup rather than the first password reset at 3am. Locale fallback is **per part**: a missing `de.html` falls back to the default while a present `de.subject` is still used — falling back whole-template is how a German user gets an entirely English email because the HTML was unfinished. HTML auto-escapes every interpolated value; text and subject do not |
| `src/shared/mailer/memory.ts` | **Not a test double** — it is what lets `STORAGE=memory` finish `identity`'s verification flow with no Docker, the same role the memory event bus plays for `I1`. Logs the link at debug so it is retrievable from a terminal; debug rather than info because a link at info is a token in a production log aggregator. Also holds `noopMailer`, which drops and **still validates** |
| `src/shared/mailer/smtp.ts` | `nodemailer`, confined here by `S10`. Maps failures to a `Kind` that says whether retrying could help — 4xx and a refused connection are `Unavailable`, 5xx is `Invalid`, a socket timeout is `Timeout` — which is the distinction that lets `retry` and `breaker` work rather than treating every mail outage as permanent. Names the host and status, **never the credential** |
| `src/shared/mailer/mailertest.ts` | One contract suite, all three adapters. The receipt shape, retrievability of both rendered parts, every problem reported at once — and **five cases on header injection**, including that the `none` adapter validates too |
| `src/shared/mailer/index.test.ts` | 30 tests. Template compilation failing at boot, per-part locale fallback, HTML auto-escaping while text does not, the debug link, and the contract against `memory` and `none` |
| `tests/testx/mailpit.ts` | Mailpit for the harness — **a file beside `postgres.ts`, not a redesign**, which is what `../MODULES.md` §3 asks as substrate lands. Reads messages back through Mailpit's HTTP API, in two calls because the list endpoint carries headers only and asserting on a summary would prove the subject arrived and nothing else |
| `tests/integration/mailer/smtp.test.ts` | 15 tests against a real Mailpit. The shared contract, plus what only a real server shows: both parts actually arriving, no `Bcc` on the wire, an unreachable host mapping to `Unavailable`, and the password absent from the error. Includes the case that closes the security loop — the injected message reached **nobody**, which a sanitising implementation would fail while passing every rejection case |
| `src/shared/httpclient/index.ts` | **L2, and the last of the layer.** The outbound mirror of `httpx`: a **per-attempt** timeout (a 30s budget spent as three 10s attempts is a different thing from one 30s attempt), retries of only what is safe to replay, `Retry-After` honoured over local backoff, provenance on the wire with the **actor opt-in**, status → `Kind` with the upstream body never in the message, and bodies capped. Note: `notes/patterns/httpclient.md` |
| `src/shared/httpclient/policy.ts` | The two decisions that look obvious until they are wrong in production. **Replay safety**: the idempotent methods, or any request carrying an `Idempotency-Key` — a bare `POST` is never retried, because replaying a charge because a response was slow is worse than failing it. **What counts against the circuit**: only unreachable-or-5xx, since a 4xx means the endpoint is *up and rejecting you* and opening on it removes a working dependency because somebody typed a bad id |
| `src/shared/httpclient/client.ts` | `breaker`'s **first real caller** — per host, never global. An open circuit fails immediately **without consuming a retry**, which needed the `details.circuit` marker added to `breaker`, since its rejection is `Unavailable` and a retry loop would otherwise burn every attempt against a circuit refusing precisely to stop the traffic. Bodies are read through a capped stream because `Content-Length` is a claim, not a limit |
| `src/shared/httpclient/index.test.ts` | 28 tests against a **real local server**, not a mocked `fetch` — a stub asserts what the code asked for, a server asserts what went on the wire. Covers the per-attempt timeout firing three times, a POST not retried and a PUT retried, `Retry-After` beating local backoff, correlation and `traceparent` present while the actor is absent unless opted in, an oversized body capped rather than read, and the breaker opening, admitting **exactly one probe**, and closing on its success |
| `src/shared/classification/index.ts` | **L3, and first of the layer — the ordering is the point.** A sensitivity vocabulary and nothing else: no encryption, no masking, no policy. It says a field is PII; `fieldcrypt` decides encryption, `redact` decides printing, `retention` decides deletion, `exports` decides visibility. Built before its six consumers because otherwise there are six answers to *is an email address PII?*, and the specific failure is the export path disagreeing with the log path. Note: `notes/patterns/classification.md` |
| `src/shared/classification/level.ts` | Five levels, **closed** like `errors.Kind` and for the same reason — a level reaches canonical bytes and generated catalogs, so growing the set is a canonical-form change. **Ordered**, so a consumer asks *at or above `pii`* rather than enumerating and a later level does not silently narrow existing checks. `UNCLASSIFIED` is `regulated`: an unlabelled field is **not public**, because guessing low means data leaves the building looking compliant and only guessing high is recoverable |
| `src/shared/classification/registry.ts` | **Declaration, never inference** — a guesser that sees `email` and infers PII also sees `email_template_id` and errs in the direction that leaks. `classify<T>` takes a `Record<keyof T, Level>`, so adding a field and forgetting to classify it **does not compile**; that is `M9` enforced by the type system. Decorators were the alternative and `erasableSyntaxOnly` forbids them, which is the better constraint — a decorator is opt-in per field, an exhaustive record is opt-out |
| `src/shared/classification/redaction.ts` | **The layering, resolved rather than moved.** `redact` is L0 and this is L3, so `redact` cannot import it and moving `redact` up would drag `logger` with it. *Redaction is a mechanism; classification is a vocabulary* — so the vocabulary is supplied here and handed down as data. Normalises field names because `redact` matches a normalised key against a raw fragment, which had `displayName` in the sensitive list and printing in full |
| `src/shared/classification/index.test.ts` | 19 tests. The ordering, the closed set round-tripping through JSON and `digest`, declaration never inferring, an unclassified field being at or above **every** threshold, and the retrofit redacting a field `redact` alone would have printed |
| `tests/rules/fixtures/semantic/m9-classification-assertion/src/contexts/identity/domain/classification.ts` | Trips `M9` — an `as` assertion defeating the exhaustive record, which is what the type system cannot catch and what silently reintroduces an unlabelled field. Landed with **no contexts in the repository**, so the rule checks an empty set today |
| `src/shared/authz/index.ts` | **L3.** Subjects, actions, resources and a decision. Knows nothing about users, which is what keeps it domain-free while `identity` is a context. Note: `notes/patterns/authz.md` |
| `src/shared/authz/subject.ts` | **The Subject is an explicit parameter, where provenance is ambient — and the asymmetry is the module's reason to exist.** Nothing branches on provenance, so a missing one costs a correlation id; a missing authorization check **grants everything**, and an ambient subject makes the forgotten check identical to the passed one at every call site. Also `Command<In, Out>`, which puts the subject in the signature so omitting it does not compile — the type-system half of `M4` |
| `src/shared/authz/policy.ts` | Policy is **data owned by the composition root**: contexts name permissions, the root maps role → grants, and it is **validated at boot** against the actions contexts declare. A grant naming an unknown action would otherwise present as a permission that never applies — the hardest failure to notice, because the endpoint denies and everyone assumes that is intended. `own` and `any` are one action at two reaches, never two action names that drift apart |
| `src/shared/authz/authorizer.ts` | **Deny by default, structurally**: no grant denies, and an unwired authorizer is `denyAll` rather than `allowAll`, so a forgotten wire is 403s in the first test rather than an open admin endpoint — the reference case for `I9`'s fail-closed column. **Scopes only ever subtract**: a key's permission is the intersection with its owner's grants, so a leaked key cannot exceed the human it belongs to. The `before` hook runs **ahead of grants** so `tenant`'s fence lands as wiring rather than restructuring |
| `src/shared/authz/index.test.ts` | 27 tests. Deny-by-default including a `denyAll` admin, own-scope seeing only its own while unrestricted sees the tenant's, scopes intersecting rather than unioning (and an *empty* scope list permitting nothing, which is not the same as absent), an unknown action denied rather than erroring, policy validation rejecting an unknown action at boot, and a `@ts-expect-error` proving a command cannot be declared without a Subject |
| `tests/rules/fixtures/semantic/m4-command-without-subject/src/contexts/identity/app/command/delete-user.ts` | Trips `M4` — an exported command in `app/command/` with no `Subject` parameter. Landed with **no use cases in the repository**, so it asserts nothing today and needs no allowlist; that window closes when `identity` arrives |
| `src/shared/tenant/index.ts` | **L3.** A tenant on provenance, a registry with two adapters, a resolver, a fail-closed accessor and a fence. Note: `notes/patterns/tenant.md` |
| `src/shared/tenant/tenant.ts` | **Single mode is byte-identical to no tenancy** (conformance 21) — one seeded tenant, zero lookups. The default tenant is a **real tenant**, not a `null` threaded through every call site, which is what keeps single mode on the multi-tenant code path. An id must satisfy the provenance id shape: `PROVENANCE.md` §7 makes it L3's obligation to mint ids conforming to a rule owned by L1, because the id reaches hashed bytes |
| `src/shared/tenant/registry.ts` | **Resolution before authentication** (conformance 22) — header or host subdomain, unknown 404, suspended 403. Resolving after auth evaluates a credential from tenant A in tenant B's context, and the session lookup, role expansion and audit row all succeed. Only a single subdomain label resolves, so an attacker-chosen `Host` resolves nowhere. `requireTenant` gives `Internal` rather than a default, and `invisible()` is 404 — a 403 confirms existence and turns any id into an oracle (conformance 23) |
| `src/shared/tenant/memory.ts` | The `STORAGE=memory` registry, seeded — a single-mode process still needs its one tenant to exist |
| `src/shared/tenant/postgres.ts` | The real registry. Seeds `default` **in the migration** rather than at boot, so a fresh database is usable before any code runs. An unrecognised status is treated as **suspended**, because a row set to a value this build does not know must not be usable |
| `src/shared/tenant/fence.ts` | **The fence is not a grant** (conformance 24). It runs ahead of any grant lookup via the `before` hook `authz` left open — a fence that were merely the most powerful grant could be out-granted by one policy edit, with no code change and no review that looked like a security change. Does not fence a resource naming no tenant, since a pre-resource question is not a fence question |
| `src/shared/tenant/tenanttest.ts` | One contract suite, both adapters — *the filter is in the SQL* and *the filter is in the memory predicate* are two implementations of one promise. Single mode proved by a **counting registry asserting zero calls** |
| `src/shared/tenant/index.test.ts` | 25 tests. The fence beating an unrestricted admin grant — proved by running the same request through an authorizer with and without it — invisibility being `NotFound` and never `Forbidden`, and the accessor giving `Internal` rather than a default |
| `tests/integration/tenant/registry.test.ts` | 19 tests. The shared contract in SQL, the migration's seed, an unknown status failing closed, cross-tenant invisibility at the storage layer, and `app.tenant_id` being set inside `withinTx`, absent without an ambient tenant, and not leaking onto the next transaction |
| `tests/rules/fixtures/semantic/m3-query-without-tenant/src/contexts/identity/infra/postgres/users.ts` | Trips `M3` — a repository query with no tenant filter. Worth a rule rather than a convention because the violation **does not error**: nothing goes red, the endpoint is fast, the tests pass, and a customer finds it |
| `src/shared/crypto/index.ts` | **L3.** Keys and the artefacts they produce. The shape matters more here than elsewhere because **the artefacts outlive the code** — a design mistake is still in the database years later. Note: `notes/patterns/crypto.md` |
| `src/shared/crypto/keyring.ts` | **The keyring is the module**; the primitives are library calls. A ring **per purpose**, so a compromised MAC key does not become a compromised signing key and so the three can rotate on different schedules. One current key, any number of retired — **rotation is adding a key, never invalidating old ones**. Nothing prints material: a keyset problem reports the *length*, and `toJSON` yields ids, the same trick `Provenance` uses. `ephemeralKeyring` is what lets `STORAGE=memory` run with nothing installed, and it marks itself so the root can warn that encrypted data is lost on restart |
| `src/shared/crypto/aead.ts` | `v1.<kid>.<nonce>.<ct>`. The `v1` costs four bytes and is the difference between a migration and a rewrite later; **the kid is the single decision that makes rotation possible** rather than trying every key and never being able to remove one. **AAD is a required parameter, never defaulted** — a ciphertext not bound to its row can be moved between rows and decrypt *cleanly*, so two users' fields swap with no error anywhere. Binds tenant, table, row and **field**, because two columns on one row swap as easily as two rows. One message for every failure, since distinguishing them is an oracle |
| `src/shared/crypto/mac.ts` | HMAC-SHA256 for challenge tokens and session fingerprints — which is why `crypto` precedes `identity`. `verify` returns a bare `boolean`: a malformed token, an unknown key and a wrong tag must be indistinguishable. Constant-time comparison, with the length checked first because `timingSafeEqual` throws on a mismatch and a tag's length is not a secret |
| `src/shared/crypto/sign.ts` | Ed25519, with the material stored as a **32-byte seed** rather than a DER key — the pair derives deterministically, so the same seed gives the same public key in Go, Python and here, which is what makes a signature cross-language verifiable. `publicKeys()` returns **retired keys too**, because last year's signature is unverifiable otherwise |
| `src/shared/crypto/derive.ts` | HKDF, so one ring serves several uses without new key material — one thing to rotate rather than two to keep in step. The `info` string is the separation, so an empty one is refused. No salt: the input is already uniform key material (RFC 5869 §3.1), and a per-call salt would make derivation non-reproducible |
| `src/shared/crypto/index.test.ts` | 36 tests. Round-trip under current, **decrypt under a retired key — the rotation test most implementations skip** — a ciphertext refused when moved to another row, field or tenant, two encryptions of one plaintext differing, an unknown key id failing cleanly, and constant-time MAC verification. The AAD and key-id properties are both verified failable: emptying the AAD turns four cases red, ignoring the kid turns the rotation case red |
| `src/shared/flags/index.ts` | **L3, and the last of the layer.** Config is read once at boot and changing it needs a restart; a flag is read **per request** and changes without one — that difference is the entire justification, and a value that never changes at runtime belongs in `env`. Note: `notes/patterns/flags.md` |
| `src/shared/flags/rule.ts` | Selectors **ANDed**, first match wins, in order. **Rules rather than a DSL**: the cost of an expression language is that nobody can reason about what is enabled for whom, and a flag nobody can reason about is worse than a deploy. If a rule needs logic beyond AND, that is two rules. `Status` distinguishes `unknown` from a decided `fallback`, so a misspelled key is visible rather than looking like somebody's deliberate off |
| `src/shared/flags/port.ts` | Reads tenant and actor from the **ambient carrier** — an observer, not a stamper — and takes everything else explicitly. **An unknown key is off**, because a typo must disable a feature rather than enable one. Every evaluation lands on a span with key, value, status and rule: a flag decision that is invisible is a debugging session where nobody can explain the behaviour |
| `src/shared/flags/static.ts` | Flags as code, changed by deploy. The right answer more often than it looks — it cannot be stale, cannot fail, and is reviewable in the same diff as the code it guards |
| `src/shared/flags/file.ts` | Polled JSON: changed without a deploy, per-instance rather than fleet-wide. Keeps serving the last good set when an edit is malformed, which is the failure that otherwise makes people stop trusting the provider |
| `src/shared/flags/postgres.ts` | Fleet-wide, TTL-cached, and **serves stale while refreshing behind**. A flag check is on the hot path and must never wait on a database — a request that blocks on a lookup has made the flag worse than a restart. The consequence shapes every test: the first read after a write is *expected* to be stale |
| `src/shared/flags/flagstest.ts` | One contract suite, all three providers. Unknown-is-off and reported unknown, first match wins in order, selectors ANDing rather than ORing, variants, and `explain` over the whole set. Freshness is asserted per provider, where the promise differs |
| `src/shared/flags/index.test.ts` | 49 tests. Cohort stickiness, monotonicity, the 0 and 100 boundaries decided without hashing, a subject-less scope excluded rather than included, the ambient carrier supplying scope, spans on every decision including unknown keys, and stale-then-refresh asserted rather than raced. The **collection's** 11 cohort vectors are read in place from `../conformance/fixtures/vectors/` — not copied, and no longer generated here — and the input is constructed from `flag` and `subject` because the fixture carries no `input` field: the string holds a raw `0x1f`, which is invisible in a committed file and mangled by editors |
| `tests/integration/flags/postgres.test.ts` | 11 tests against a real database. The shared contract, plus what only this provider promises: **serve stale, then the new value** — asserted by **retry rather than sleep**, because the promise is *eventually, within the TTL*, and sleeping picks a number and hopes. Also that a cold cache answers immediately rather than reaching for the database, and that a database that has gone away keeps the last good set rather than turning every flag off |
| `src/shared/flags/cohort.ts` | **The cross-language one, and not this repo's to decide.** Collection decision 0009 fixes it: `be_uint32(sha256(key + U+001F + subject)[0..4)) % 10000`. **The separator can never be revised** — changing it after a rollout is live reassigns every bucket, so the 10% cohort becomes a *different* 10% mid-experiment. U+001F rather than `":"` because it cannot occur in a key or subject, so the encoding is unambiguous by construction and **there is no rejection rule**; under `":"` the natural call site — bucketing on `Actor.String()`, which is `"kind:id"` — was rejected on every request while `flags` failed closed. `uint32` because it is exact in JavaScript. 0 and 100 decided without hashing |
| `tests/testx/probe.ts` | Is there a database to integrate against? Short connect timeout on purpose — the case being measured is the one where nothing answers, and that is the case that must not cost anybody a minute. Reports `host:port/database` and never the credentials, since the reason gets pasted into a report |
| `tests/testx/global-setup.ts` | Runs the probe **once per run**, in the main process, and hands the answer to every worker through `provide`. The first version probed at module scope in the gate, which meant once per *worker*: a connection attempt and a duplicate line for each one vitest span up, on the machine least able to afford it |
| `tests/testx/gate.ts` | Skip with a reason, never fail, when there is no database. Rung 0 is the rung that needs nothing (`../INFRASTRUCTURE.md` §1), and a suite that goes red on a fresh clone with no Docker is a suite people stop reading — after which a real failure in it is indistinguishable from the usual noise. The reason goes in the suite title, which the verbose reporter prints, **and** once on stderr, because the default reporter prints only a count and a skip whose cause is invisible reads as a pass |
| `tests/testx/process.ts` | **The real binary, as a child process.** Rung 3 is *journeys against the real binary* — not the handler, not a wrapper around an in-process app. Everything else in this tree was green while `src/main.ts` mounted no routes at all: unit suites called handlers directly, contract suites called adapters directly, and nothing asked whether the process answered HTTP. A suite that imports the app cannot fail that way, so it cannot catch it either. Started with `PORT=0` and waits for the `ready` line rather than polling, because the bound port is only knowable from what the process printed; stopped with `SIGTERM`, because the graceful path is part of what rung 3 tests |
| `tests/testx/journal.ts` | An HTTP client that writes down what it did. **A passing test is nothing anyone can look at** — this records every exchange (method, path, headers, body, status, response, `request_id`) and writes `artifacts/e2e-journal.md`, so `make e2e` produces a document rather than a tick. Credentials are aliased on the way *in* and again at render, since a token minted in step 3 is not a secret yet when step 2 is recorded; the alias is stable, so *this request reused that session* stays readable. The artifact is written whatever the verdict, because the journal of a **failed** journey is the one somebody needs |
| `tests/e2e/journey.ts` | The journey itself, written once and run against every storage mode. Register → log in twice → `/me` → change the password → the other session dead and this one live → log out → the token refused → the audit records that journey produced, with the correlation id tying each back to the request that caused it. Only the audit reads retry, and the reason is the point: in memory a subscriber runs inside the publishing transaction, and in Postgres the outbox relay delivers a moment later — a fixed `sleep` would be the flake, so it waits for the condition |
| `tests/e2e/journey.test.ts` | The journey in memory mode. **Zero external dependencies** — invariant `I1` — so it runs on a fresh clone with no Docker, which is what makes it the one nobody is entitled to skip |
| `tests/e2e/journey-postgres.test.ts` | The **same** body against a real database, skipping with a reason when there is none. Applies migrations by running `main.ts migrate` rather than importing the migrator, because the list it applies was `[]` while every test was green and only the binary reads it |
| `tests/e2e/bootstrap.test.ts` | **The first administrator** — `../CONTEXTS.md` §7.4. Granting a role needs a role, so an empty database cannot reach one; this proves the base case the only way it can be proved, by running the real `seed` command and then reaching a route no registered user can. Runs `seed` **twice**, because a seed that is not idempotent breaks the second deploy rather than the first, and asserts the refusal exits `78` — a default administrator password is the same credential in every deploy and the `.env` gets copied |
| `tests/integration/postgres/storage.test.ts` | The storage suite against a real PostgreSQL. One adapter today; §3 nominates this repository for the two-adapters-under-one-repository-port experiment, and when the second lands this file gains a second call and nothing else |
| `tests/integration/postgres/migrate.test.ts` | Migrations against a real database — the half `postgres` is not ticked without. Idempotence, per-context namespacing, a refused edit to an applied migration, two migrators started together serialising on the advisory lock, and a failed set leaving the database exactly as it was |
| `tests/integration/postgres/guardrails.test.ts` | That the three timeouts actually **fire**, not merely that they are set — a setting applied to the wrong session, or after the first query, looks identical to one that works until the day it matters. Includes the asymmetry: a migration is exempt from the statement budget and not from the lock one. Found both pool defects listed above |
| `tests/integration/postgres/db.test.ts` | §3's acceptance test, by name: one repository whose signature names `DB`, handed **a pool, a transaction, and `testx`'s per-schema pool**, unchanged. There is no second version for the transactional case, and that absence is the assertion |
| `src/shared/idempotency/index.ts` | **L4, position 9 of the chain.** Claim before running, replay stored responses bit for bit, fail closed. The wiring is one line because `httpx` left the slot named and empty; the module is the work. Note: `notes/patterns/idempotency.md` |
| `src/shared/idempotency/key.ts` | **The key is scoped, never global** — the client's `Idempotency-Key` plus the tenant and the authenticated principal. A bare key is a cross-tenant read: guess another tenant's key and you get their response body, and neither party can tell. This is *why* position 9 sits below authn and tenant — it cannot build its own key until those have run. An anonymous caller here is `Internal`, not 401: position 6 already refused anyone a route required to authenticate, so reaching position 9 anonymously means the route is **public**, and no client-facing status would be true of it (ADR 0010). The fingerprint is a digest of the **canonical** request — method, path and RFC 8785 body — because raw bytes turn a client whose JSON library reorders keys into case 26's 422, punishing the safe retry the mechanism exists to protect |
| `src/shared/idempotency/port.ts` | Five outcomes, closed: `claimed`, `replay`, `mismatch`, `in-flight`, `consumed`. Collapsing any two loses a case by name. `consumed` is the key spent past the storage cap — finished, with nothing to replay. **Two clocks in two columns**: `expiresAt` governs how long a completed response replays, `leaseUntil` how long an in-flight claim is honoured. Without the lease a process dying between claiming and storing leaves the key in flight forever and case 27's 409 becomes **permanent** — a client locked out of an operation it never completed. Merged, completing a request either holds a crashed claim for a day or expires the response with the lease |
| `src/shared/idempotency/release.ts` | **A rule that was wrong once, and how it was wrong is more useful than the rule.** The first version was *5xx releases, 4xx holds*, and it held up against every case anybody had written down — until `conditional` produced a 4xx that is nothing of the sort. The status class was never the test, it was a **proxy** for one. The test is two questions: *would re-execution answer the same*, and *did anything happen*. A server fault might differ and might have happened — release. **A 412 will differ and definitely did not** — release, and safe for a reason specific to 412 rather than a loosening: `conditional` sits inside this module and throws before calling `next`, so nothing executed. A 400 or 422 answers the same way every time — hold. `canceled` is the row proving the two questions are separate: it might differ, and it is still held, because a caller who hung up says nothing about whether the handler finished. Under the old rule a client that re-read and corrected its validator was **stranded**, forced to invent a new key to make progress on a request it had already fixed |
| `src/shared/idempotency/middleware.ts` | The three rules most likely to be "simplified", documented where somebody is standing when they decide to. Imports the chain vocabulary from the **floor**, not from `httpx`. Refuses the anonymous pairing at **startup** — `anonymousCallers: 'permitted'` throws at construction, because a fact about the mount is not a fact about a request. The release rule has its own file — see `release.ts`. **The store fails closed**: unreachable is 503, never proceed-unclaimed, which is the exact opposite of `ratelimit`'s default in position 7 and right for the same reason it is right there. A quieter asymmetry inside: failing closed at *claim* time prevents an execution, failing at *store* time would deny one that already happened, so a `complete` that throws is reported and the response returned |
| `src/shared/idempotency/capture.ts` | An **allowlist** of headers the response owns, because a replayed `set-cookie` hands a second caller the first caller's session and a replayed `x-request-id` points a support conversation at somebody else's log line. Past the cap the response **passes through** — it is a real answer and a 500 would tell the client its write failed when it did not — and the failure is logged at error level naming the endpoint, the size and the cap. Silently declining to store leaves the client believing it is protected. The key is **consumed, never released**: releasing means the retry re-executes and double-applies the write, and *losing replay is a cost, losing the guarantee is a failure* |
| `src/shared/idempotency/memory.ts` | `STORAGE=memory`, invariant `I1`. Atomic by construction — JavaScript runs the claim to completion — and it says plainly that this proves nothing about a statement racing another process, which is what the PostgreSQL adapter is chosen for |
| `src/shared/idempotency/postgres.ts` | The atomic claim: `insert ... on conflict do update ... where`, taking the key when nobody holds it and back when the existing row is dead. **Sixteen concurrent claims against a real database produced three winners on the first run** — not the upsert's fault but the branch that read "the follow-up select returned nothing, so nobody holds it". `on conflict` re-checks the latest row version while the select beside it keeps the statement's opening snapshot, so a claimant blocked on an uncommitted insert wakes to a correct conflict and a correct empty read. Empty now means *something blocked us that we cannot see*, resolved by a second read on a fresh snapshot and, failing that, a 409. Nothing in-process could have found it |
| `src/shared/idempotency/schema.ts` | `primary key (tenant, principal, key)`. `consumed` is its own column rather than a sentinel status, because *spent* and *answered 204* are different facts and a reader of this table needs to tell them apart. A bare `key` primary key looks like a schema simplification in review and **is** the cross-tenant read, which is why the integration suite asserts the key columns rather than trusting them. The expiry index is not partial, for the outbox's reason: `now()` is STABLE, not IMMUTABLE, and SQLSTATE 42P17 is what a partial predicate earns |
| `src/shared/idempotency/idempotencytest.ts` | One contract suite, both adapters. Conformance 25-28, the cross-tenant and cross-principal cases that nothing else catches, the crash test — claim, never complete, reclaimable after the lease and not before — and the case that fails if the two clocks are merged. **Real waits, not a fake clock**: one clock lives inside PostgreSQL as `now()` where no injected clock reaches, so faking time would assert the memory adapter's arithmetic twice and the thing that matters never |
| `src/shared/idempotency/index.test.ts` | 51 tests. The contract against memory, then the middleware through the **real chain**: 25-28 end to end, the release table as tests rather than comments, a re-serialized identical payload that must *not* be 422, `set-cookie` and `date` absent from a replay while `content-type` survives, the cap failing loudly and keeping its claim, and an anonymous caller refused with the handler never running. **Two cases wire `conditional` in as well**, because neither module's own suite can reach the interaction: a client whose corrected `If-Match` lets it make progress on the same key — which fails under the old *4xx holds* rule — and a replay that still returns the stored response after the state has moved, which fails if `conditional` is moved outside |
| `tests/integration/idempotency/postgres.test.ts` | 21 tests against a real database. Sixteen genuinely concurrent claims with exactly one winner; a crashed claimant reclaimed on the **database's** clock; and the snapshot race forced deterministically with two sessions — one holds an uncommitted claim, the other blocks on it, the first commits, and the second must answer `replay` rather than a defensive 409. That last test is the one that proves the fresh-snapshot re-read earns its round trip |
| `src/shared/ratelimit/index.ts` | **L4, position 7 of the chain**, and the last of the three slots `httpx` left named and empty. Claim a token or be refused, per caller, against **shared** state. Note: `notes/patterns/ratelimit.md` |
| `src/shared/ratelimit/bucket.ts` | The token bucket as arithmetic, shared by both adapters so *what a bucket does* is decided once and only *where the state lives* varies. **Rule `M13` lives here**: elapsed is floored at zero, so a clock stepping backward refills nothing rather than *draining* the bucket and turning a correction into a throttle nobody configured; and the result is capped at capacity, so a clock stepping forward grants one full bucket rather than an unbounded burst. `Reset` means *how long until your next request is admitted* — a deliberate reading, because a continuously-refilling bucket has no window boundary to point at, and because it makes `Retry-After` agreeing with it **structural** rather than two code paths that are supposed to match |
| `src/shared/ratelimit/key.ts` | **The caller is the principal**, which is why position 7 sits below authn — a limiter that ran first would have no caller to key on (case 40). The peer address is the anonymous fallback, prefixed by kind so an address cannot collide with an actor id. **A forwarded-for header is trusted only when a proxy is configured as trusted, and the default trusts nothing**: trusting it hands every caller a limit-evasion primitive, and worse, lets one caller **exhaust another's** bucket by forging their address — a throttle turned into a tool aimed at one victim. `trustedProxyHops` counts from the **right**, because each hop appends; taking the leftmost is the classic bug and the one that reads most naturally. A chain shorter than the configured topology yields nothing rather than a clamped guess. The tenant is absent from the key because position 8 has not run yet |
| `src/shared/ratelimit/trust.ts` | **CIDR prefixes, never a hop count.** This replaced `trustedProxyHops: number`, and the two are not refinements of each other — they fail in opposite directions. A hop count is positional: `1` means *the last entry is the client*, so adding a proxy or letting one request arrive directly makes it read an address the caller wrote, with the header present, the count satisfied and nothing looking wrong. Walking a prefix set fails **closed** in the same situations. `X-Real-IP` is gated by the same set and loses to `X-Forwarded-For`, which carries a chain that can be validated. **No default: unset refuses to boot** — trusting by default is a limit-evasion primitive, not trusting by default makes the limiter global behind any load balancer, and `none` is the legal explicit answer. Maps IPv4-mapped IPv6 to its four bytes, because that is what Node reports on a dual-stack listener and without it a trusted set silently matches nothing |
| `src/shared/ratelimit/port.ts` | Why this is a port at all: **a per-process bucket is not a rate limit.** Four replicas each admitting the configured rate admit four times it, and every single-instance test passes |
| `src/shared/ratelimit/middleware.ts` | **Failing open means degrading, not switching off.** A store outage falls back to a per-process bucket sized for one replica's share — *the store is unreachable* and *there is no limit* are different facts, and an outage is exactly when an unlimited edge is most dangerous, because whatever took the store down is usually load. The fallback is built **once**, not per request: rebuilt per request it is a full bucket per request, which is no limit at all wearing a limit's shape. Logged once rather than once per request, with a recovery line, because `I9` wants a fail-open choice visible and an outage should not also be a log flood. **The exact opposite of `idempotency` in position 9**, and right for the same reason it is right there — `../RESILIENCE.md` §1 decides by category. Liveness and readiness are never limited, and exempt means *not counted* rather than *counted and forgiven* |
| `src/shared/ratelimit/headers.ts` | Case 39's separate-header form. On **every** response, not only a 429: a client that learns its budget only by exceeding it has to exceed it to learn anything. `Retry-After` is rendered from the same value as `RateLimit-Reset` so the two cannot drift, and never zero — telling a refused client to retry immediately is an instruction to hammer |
| `src/shared/ratelimit/memory.ts` | The memory twin **and** the degraded fallback, one implementation serving both. The store is a value separate from the limiter on purpose: a factory hiding its own map in a closure could not express *two limiters over one store*, so the contract suite could not have asked the question that matters. This is the adapter that satisfies `M13` exactly — the reading is `clock.elapsed()` |
| `src/shared/ratelimit/postgres.ts` | Refill, decide and write in **one statement**, so two concurrent requests cannot both observe the last token. The refill expression appears twice — in `set` and in `where` — deliberately: `on conflict do update`'s `where` reads the **latest row version**, and moving the arithmetic elsewhere would silently move it onto the statement's snapshot, which is the distinction that cost three of sixteen claimants in `idempotency`. An empty result means *something we cannot see*, and here it resolves to **admit** — the inverse of that module's answer to the identical situation, because this one's category is availability. **PostgreSQL exposes no monotonic clock**, so `M13` is honoured by the clamps in `bucket.ts` rather than by the reading: a clock correction on the database host costs at most one burst and never a stall |
| `src/shared/ratelimit/schema.ts` | One row per caller, and the row **is** the bucket — no history, because a limiter needs to know how many tokens you have, not how you spent them. `tokens` is `float8`: rounding on every write floors away the fraction that had just accrued, so a steady stream of requests keeps a bucket at zero forever and it looks like the limit working |
| `src/shared/ratelimit/ratelimittest.ts` | One contract suite, both adapters, and **two limiters over one store** is the case that earns its place — every other case here passes against a private per-process map. The window is chosen by the **adapter**: the memory twin drives a fake clock and a ten-second window is free, while the shared adapter has to wait, so every case is written in fractions of the window rather than in seconds |
| `src/shared/ratelimit/index.test.ts` | 46 tests. The contract against memory, the M13 pair — a wall clock moved a year in each direction while the monotonic reading stays put — the trusted-proxy default and the forged-victim case, `Retry-After` agreeing with `Reset` by construction, and the middleware through the **real chain**: a 429 that is an RFC 9457 body with a request id and `err_kind`, headers on a 200 and on a 500, the degraded fallback still limiting at one replica's share, health endpoints neither limited nor counted, and a **replayed** idempotent request still spending budget |
| `tests/integration/ratelimit/postgres.test.ts` | 22 tests against a real database. Forty genuinely concurrent takes with exactly five admitted; four independent limiters sharing one budget; refill on the **database's** clock; one row per caller under a burst; and the fractional-token assertion read straight off the column, drained well below the cap first because refilling into a full bucket clamps to a whole number and would prove nothing |
| `src/contexts/identity/index.ts` | **The context root, and the only way in** (`../CONTEXTS.md` §8 step 6). Exports the handler, the position-6 authenticator, `conditional`'s validators, the declared routes for `openapi` to walk, and the migrations. `STORAGE=memory` when no `db` is passed — a real mode, not a demo. Note: `notes/domain/identity.md` |
| `src/contexts/identity/domain/index.ts` | The domain's root, and the one place `S7`'s consequence is stated: `result` is not importable here, so value objects **throw** rather than returning one. `ARCHITECTURE.md` §L0 calls `errors` *and* `result` the kernel's vocabulary while `ENFORCEMENT.md` `S7` permits only `errors` — a gap that matters in a language with a `Result` type and not in Go. Followed as written, raised rather than widened |
| `src/contexts/identity/domain/ids.ts` | Four typed ids, branded **locally** because `S7` permits `domain/` exactly one import and `brand` is not it. Four lines against an architectural boundary worth keeping: a domain that may import one helper may import two |
| `src/contexts/identity/domain/email.ts` | Normalizes at construction, never at use (§7.2). **Lowercases the whole address**, not just the domain — formally the local part is case-sensitive, in practice no provider treats it so, and preserving the distinction lets `Bob@x.com` register while `bob@x.com` is taken. A deliberately loose shape check: RFC 5322's grammar admits addresses no provider accepts, and the only proof an address exists is that mail to it arrives |
| `src/contexts/identity/domain/password.ts` | **`Password` is a class, not a branded string**, and that is forced by §2.1's *redacts in every string form*: a branded primitive prints itself into any template literal or `JSON.stringify`. `toString`, `toJSON` and Node's inspect hook are all covered; `reveal()` is named so reading the secret is visible at the call site. **No composition rules** — NIST SP 800-63B has advised against them since 2017 — with the ceiling as a denial-of-service control rather than a strength one. `PasswordHash` is opaque: empty means **no password**, which is §2.2's *a user is not their credentials* as a type |
| `src/contexts/identity/domain/role.ts` | Identity records *which* roles; the root's policy decides what they permit. Normalized and sorted, so a grant that changed nothing does not bump `Version` and the ETag over a user stays stable across a round trip |
| `src/contexts/identity/domain/user.ts` | Every mutation bumps `version` and `updatedAt`. **`baseVersion` is a separate field**: a repository writes on `(id, baseVersion)`, and `where version = version - 1` is right for one mutation and silently wrong for two. Idempotent `disable`/`enable` **report whether they changed anything**, which is what lets a command publish the event exactly once. `assertCanAuthenticate` lives on the aggregate so every future authentication method inherits case 11 without knowing it made a check |
| `src/contexts/identity/domain/session.ts` | **Two aggregates, not `User.Sessions`** (§2.2): sessions are written far more often and modelling them as a collection would bump the user's version on every login, making the concurrency token useless for what it is for. **`Session.issue` is the convergence point** — every authentication method ends here, and `method` names which. Only a **fingerprint** is stored. Revoked beats expired, so an operator revoking an already-expired session sees *revoked*. `touch` is throttled to a minute: unthrottled it is a write on every authenticated request |
| `src/contexts/identity/domain/challenge.ts` | **One aggregate, many purposes** (§2.2, §7.5) — the issue-and-consume rules are identical and only the after-effect differs, which is a handler rather than an aggregate. Single-use, TTL'd, **purpose inside the MAC** so a reset secret cannot be replayed as a magic link, and idempotent by source event id (§7.7). `consume` returns nothing about *why* it failed: case 13's one indistinguishable error, expressed in a signature |
| `src/contexts/identity/domain/apikey.ts` | Shown once, never returned — case 16 is true because there is nothing to return. Carries an **`ak_` prefix** so a leaked key is findable by a secret scanner and one bearer scheme can carry two credential kinds. **Empty scopes mean the key can do nothing**, not everything — an empty list read as unrestricted is the expensive way to get case 17 wrong |
| `src/contexts/identity/domain/events.ts` | §2.5's names, and they are a **cross-context contract** rather than this context's choice: `audit` imports no context and cannot look anything up. `<context>.<aggregate>.<past-tense-verb>`, subject in the payload, actor on the envelope. `SessionRevoked` carries a **reason**, because a logout, a password change, a disablement and an expiry all produce it and only the producer knows which |
| `src/contexts/identity/domain/challenge.test.ts` | The aggregate's own guards, tested where they live — **because a sweep found the app layer shadowing them**. `claim()` checks the purpose before calling `consume`, so breaking the aggregate's check changed no end-to-end behaviour and nothing failed. An untested guard is one somebody deletes as redundant |
| `src/contexts/identity/app/ports.ts` | The out-ports, declared by `app/` and injected by the root (`S8`). **`Work` is why `app/` never sees a `DB`**: §4 requires the outbox row in the *same* transaction as the data write, so publishing needs the transaction handle — this hands out repositories already bound to it. `Hasher.dummy` is a **value rather than a `verifyDummy()` method**, so case 7's two paths are one code path with a different argument |
| `src/contexts/identity/app/tokens.ts` | Mint, fingerprint, store the shadow. One helper for session tokens, API keys and challenge secrets, because three would drift and the one that drifted would be the one stored raw. A plain SHA-256 rather than a password hash: a 256-bit random token is not guessable, and a memory-hard hash on the **per-request** session lookup is a self-inflicted denial of service |
| `src/contexts/identity/app/command/authenticate.ts` | **The convergence point**, built with exactly one caller. Owns the TTL, the token, the fingerprinting, the disabled check and both events; a method supplies a `User` and a name. Hard to add later for a reason that is not technical — by then three call sites each create sessions their own way, and the one that forgot `enabled` is the one somebody logs in through |
| `src/contexts/identity/app/command/login.ts` | Case 7, and it is about **timing** as much as wording. The work happens either way against `Hasher.dummy`; a malformed address, a too-short password and a user with no password all take the same path, because each would otherwise be an oracle wearing a different status code. `AuthenticationFailed` fires **only for existing users** — an unknown address is unverified input, and publishing it writes an attacker's guesses into `audit` |
| `src/contexts/identity/app/command/register.ts` | Case 5. The hash is computed **outside** the transaction: argon2-class work inside one puts every registration in the idle-in-transaction budget for the duration of a KDF. Uniqueness is the repository's, so a concurrent signup loses at the index rather than in a read this command performed |
| `src/contexts/identity/app/query/list-users.ts` | **Keyset, never offset** — `../CONFORMANCE.md` §3.5 makes the list and detail routes non-optional, and thirteen cases depend on them. Offset re-scans everything before the page, drifts under concurrent inserts, and gives a client page numbers that stop meaning anything the moment a row is added. The key is `(created_at, id)`, total where `created_at` alone is not: two rows created in the same millisecond tie, and a cursor on a non-total order silently skips or repeats at the tie. **The filter is part of the ordering name**, so a cursor issued against an unfiltered listing is refused against a filtered one — case 33, where the same position would mean a different row. A backward page comes back descending and is reversed here rather than in the adapter, which should not answer in an order the query did not ask for |
| `src/contexts/identity/app/command/update-user.ts` | `PATCH /v1/users/{id}`, and **where enable and disable live** — §3.5 settles them here rather than as sub-resource verbs, since the route already carries `If-Match` and the domain's `Disable`/`Enable` are already idempotent. **Concurrency is checked twice on purpose**: `conditional` at position 9 refuses a stale `If-Match` before this runs, and the repository's write on `(id, baseVersion)` catches the row moving between this read and this write — a window position 9 cannot see. A third check against a version parsed out of the header was written first and removed: an ETag identifies a representation and does not encode a version, so reading one out of it is the §3 mistake in a new place. A no-op PATCH writes nothing, publishes nothing and still answers 200 — bumping a version on a request that changed nothing invalidates every cached tag for free |
| `src/contexts/identity/app/command/sessions.ts` | The revoke sweep, in **one repository call rather than a read-then-loop**: both callers are security operations, and a loop that fails halfway leaves live sessions. Counts what it **ended**, not what it found |
| `src/contexts/identity/app/command/change-password.ts` | Case 9 — revokes every **other** session and spares the caller's. Re-verifies the current password first, because without it a stolen session becomes a stolen account |
| `src/contexts/identity/app/command/challenges.ts` | Cases 13, 14, 15. `requestChallenge` returns **`void`**: case 15 says an unknown address gets the same 202, so there is no value that *could* carry the difference. The mail is sent **after** the commit — a message about a rolled-back row is a link that does not work. Case 14 revokes **all** sessions **and the user's other outstanding reset links**, which is the half that gets forgotten. `magicLinkLogin` is the second authentication method and is nine lines |
| `src/contexts/identity/app/command/roles.ts` | Case 12, which **needs no code** — the session carries no roles, so `resolveCaller` reads them fresh and a grant lands on the next request with no token to reissue. Authorization here, with an explicit `Subject` (§3 rule 6), and `denyAll` when the root wires nothing |
| `src/contexts/identity/app/command/apikeys.ts` | Cases 16 and 17. Case 17's intersection is `authz`'s; this carries the scopes and never re-implements the subtraction |
| `src/contexts/identity/app/query/caller.ts` | The per-request lookup cases 8, 10, 11 and 12 all turn on. **Fingerprint in, never a token.** One refusal for every reason — absent, unknown, expired, revoked, or a user since disabled. The disabled check is per request rather than a sweep, which would race a login in flight. The touch **fails open**: a request that failed because a timestamp write failed is an outage caused by bookkeeping |
| `src/contexts/identity/app/index.ts` | What the use cases need, in one bag. Every field is a port this context declared or a shared module's own |
| `src/contexts/identity/app/ports.contract.ts` | One suite; both adapters pass it (§8 step 4). The cases are the ones where an adapter can be plausibly wrong — the unique address, the version check, the revoke sweep, the second consume, the source-event uniqueness. Everything the aggregate decides is tested against the aggregate |
| `src/contexts/identity/infra/memory/index.ts` | `STORAGE=memory`, invariant `I1`. Stores **states, not aggregates**: keeping the aggregate would hand every caller the object the store holds, so an unsaved mutation would be visible to the next reader — which PostgreSQL would never do and one suite asked of both would never catch. **Says plainly that its `Transactor` is not atomic**, and the contract suite does not assert rollback: a suite that proved atomicity against three `Map`s would give exactly the confidence that must not be given |
| `src/contexts/identity/infra/postgres/schema.ts` | Three migrations. `password_hash` is **nullable** — the storage shape of *a user is not their credentials*. `source_event_id` carries a **partial** unique index, and this one is legal where the outbox's was not: the predicate is `IS NOT NULL` rather than anything involving `now()`, so it is IMMUTABLE |
| `src/contexts/identity/infra/postgres/index.ts` | The same suite, plus what only a database gives: a unique index that holds under concurrency, and a transaction making the outbox row atomic with the data write. `publish` gets **`tx`, not the pool** — passing the pool would silently restore the dual-write problem the outbox removes |
| `src/contexts/identity/infra/memory/index.test.ts` | The memory adapter against the shared contract |
| `src/contexts/identity/infra/hasher.ts` | **Argon2id at `m=19456`, `t=2`, `p=1`, PHC format** — collection decision 0011, which supersedes the scrypt trade this row used to describe. Parameters are stored **with the hash**, not read from a constant, so raising the cost does not invalidate every existing password. **Two shortcuts the library accepts and this must not**: a tag correct at a *lower* cost verifies happily, and a tag correct for *argon2i* verifies happily too, because the library uses the parameters in the string. The first is verified-then-flagged for rehash (the accepted upgrade path); the second is a hard reject, since the stored value is not the thing the policy asked for. Silent acceptance of either is what the fixture exists to prevent. A hash it cannot parse throws rather than answering `false` — a corrupt row is not a wrong password |
| `src/contexts/identity/infra/hasher.test.ts` | Against the **collection's** fixture at `../conformance/fixtures/vectors/password-hash.json`, read in place rather than copied — a copy is a fixture that agrees with itself. Four of its five cases must fail. One test asserts what the raw library does **unaided**, which is the whole reason the fixture exists: the requirement was stated and unenforced until it landed. This repository generated the real tags as first implementer, each computed correctly at its own parameters so every case fails for its named reason |
| `src/shared/httproute/index.ts` | **The route registry, shared rather than copied.** It began as `identity/transport/http/registry.ts` and was promoted the moment `audit` needed the same shape — collection decision: *the trigger for promotion is the second copy, not the third context*, and `scaffold` is not the answer because a generator emits copies and does not keep them agreeing afterwards. Each route declares its request and response schemas and the dispatcher validates from that declaration — §8 step 5, one source and never two, so `openapi` later walks the registry and touches no handler. Framework-neutral: routes are values folded into an `edge` handler, so the same registry mounts behind either server. **It carries no authentication**: `auth` is declared per route and the caller arrives from a function the context supplies, which is what lets `audit` require an authenticated principal without importing `identity` (`S6`). Generic in the caller type, through `routesFor<C>()` — TypeScript takes type arguments all-or-none, so a plain generic would have cost body inference at every route |
| `src/shared/httproute/statuses.ts` | **S11**, and the half a reader gets wrong. A handler's own statuses are visible in the handler; the 400 from schema validation, the 401 from the registry, the 429 from position 7 and the 412 from position 9 come from code the route never mentions and reach the client under that route's name. `ChainShape` is a **parameter**, not an assumption — a process that mounts no rate limiter cannot answer 429, and asserting it could would push every route to declare a status it never returns. Structurally typed rather than importing `AnyRoute`, because the router imports the global list back and the cruiser's `no-circular` caught the pair on the first run |
| `src/shared/openapi/index.ts` | **Generated, committed, diffed** — `../MODULES.md`'s three words, and only the first is easy. It walks the route registry and **touches no handler**: if a handler had to be annotated to make it work, the registry would be missing something and that would be the bug. It inherits `S11`, so anywhere the document is wrong is a real gap in a declaration rather than a documentation lapse. Deterministic by construction — sorted routes, fixed key order — because a generator whose output depends on registration order cannot be diffed, and a drift check that fires on every unrelated change is one somebody deletes |
| `src/shared/blob/index.ts` | The module root. Two things it is about: streaming, and keys — and the keys are the half that earns its existence |
| `src/shared/blob/key.ts` | **The validation is the module.** A key derived from user input is a path traversal waiting for a concatenation, so a key is a value object and nothing accepts a bare string. **Tenant-scoped means the key encodes the tenant** — a filter is a `where` clause somebody forgets, and the forgetting has no symptom until a customer reads another customer's export. An allow-list, because `%2e%2e`, `..%2f`, an overlong encoding and a backslash are each a separate thing a deny-list has to remember. A breakage pass deleted a redundant `.`/`..` guard: the pattern's leading `[a-z0-9]` already carried the rule, and a second place a rule appears to live is a place somebody trusts after relaxing the first |
| `src/shared/blob/port.ts` | Streaming, because an export is arbitrarily large and `put(key, Buffer)` works until somebody exports a real dataset |
| `src/shared/blob/memory.ts` | The twin. Buffers, which is the one honest difference: an in-memory store **is** a buffer, and the port stays streaming so no caller writes code that only works because the bytes were already in hand |
| `src/shared/blob/filesystem.ts` | The real adapter, and **not S3 on purpose**: `I2` wants a store with real bytes, real streaming and a key that is a path, and a directory gives all three with no account, no network and no credential — so a fresh clone can run rung 2. `realpath` catches what no string can, a symlink planted in the tree. Two findings from breaking it: the root itself is a symlink on macOS, so containment compared a real path against a possibly-linked root; and `get`/`head` swallowed a containment refusal as *absent*, so a traversal attempt was indistinguishable from a missing object |
| `src/shared/blob/blobtest.ts` | One suite, both adapters — `I2` |
| `src/shared/blob/index.test.ts` | 28 cases, most of them keys |
| `src/shared/work/index.ts` | The module root |
| `src/shared/work/port.ts` | **Enqueue in your transaction** — the clause that IS the design. The row a job is about and the queue entry commit together or neither does; an export row that rolled back leaves a job whose target does not exist. The reading is a parameter, for the reason `ratelimit` learned: an adapter consulting its own clock cannot be driven by the suite proving it agrees with its twin |
| `src/shared/work/memory.ts` | The twin, which supplies **no** `rollBack` to the contract — honestly, because it has nothing to make atomic. A rollback that did nothing would make the case pass here and prove nothing |
| `src/shared/work/postgres.ts` | The durable queue. The **lease predicate** is the correctness mechanism and `for update skip locked` is the latency one, stated separately because crediting the wrong one is how a break stops being detectable — which is what happened in `events` |
| `src/shared/work/worker.ts` | The loop, separate from the port: a queue with a loop welded on cannot be enqueued into by a process that does no work, which is most of them. Every job runs inside the provenance that enqueued it |
| `src/shared/work/worktest.ts` | One suite, both adapters. **The transactional case did not exist**, which is the failure the collection had already found twice — every other case enqueues outside a transaction and never notices the writer is ignored. Deleting `?? db` now fails two cases; before, it failed none |
| `src/shared/work/index.test.ts` | The memory queue and the worker loop |
| `src/shared/work/schema.ts` | Two tables. Dead letters are a table rather than a flag, because a job nobody can run is evidence and a flag on a row somebody purges is not |
| `src/shared/operations/operation.ts` | **A terminal state never moves** — the rule the ×3 copies disagreed about, and the reason a poll can promise conformance case 46. **Cancellation is a state with checkpoint semantics, not a kill**: nothing here can interrupt a syscall, and a module claiming otherwise would be lying to every caller that believed it. The result is a **reference**, so authorization happens at download time rather than hours earlier |
| `src/shared/operations/index.ts` | A barrel and nothing else — a class here made every adapter import the file that re-exported them, which `no-circular` caught on the first run |
| `src/shared/operations/port.ts` | Takes the caller's writer, so an operation row and the thing it is about commit together |
| `src/shared/operations/memory.ts` | The twin. Its methods are **`async`**, and the twin had to be told: a `Promise<void>` that throws synchronously cannot be `.catch()`ed, and `I2` missed it because no contract case used `.rejects` on the memory side |
| `src/shared/operations/postgres.ts` | Optimistic writes on `(id, version)` |
| `src/shared/operations/schema.ts` | One table, indexed by `(tenant, owner_id, started_at desc)` — the shape a caller's own operation list reads |
| `src/shared/operations/index.test.ts` | 16 cases, and five of them are the terminal-state rule from different directions |
| `tests/integration/work/postgres.test.ts` | **Where the transactional clause can actually fail.** The contract's `rollBack` runs only here, plus the ordering that catches a queue writing on its own connection: enqueue succeeds, the data write then fails, and a queue outside the transaction keeps a job for a row that never existed |
| `tests/integration/blob/filesystem.test.ts` | The symlink, the large body, and a traversing key that arrived from **outside** the type system — a cast, which is what a database row looks like to TypeScript |
| `src/contexts/exports/domain/ids.ts` | One typed id, branded locally — `S7` permits `domain/` exactly one import |
| `src/contexts/exports/domain/events.ts` | §2.5's names. `Downloaded` is published so an audit trail records who read an artifact, which is the question an export exists to be asked about later |
| `src/contexts/exports/domain/index.ts` | The domain's root, and the fourth context to state `S7`'s consequence |
| `src/contexts/exports/domain/export.ts` | A **thin** aggregate: the state machine of long-running work lives in `operations` and is not this context's to reinvent. What is this context's is what may be exported, in what format, and how long the result lives. **The TTL starts when the artifact is written**, not when it is requested — an export that took an hour would otherwise arrive already expired. `Dataset` has one value and it is not a placeholder: `audit` sat in the union briefly with nothing behind it, which would have shipped an endpoint that accepted a request and produced an empty file |
| `src/contexts/exports/app/ports.ts` | `Work` reaches all three stores, which is why it is one interface rather than three transactors: the export row, the operation and the queue entry are one commit. `Datasets` is the seam again — `S6` forbids importing `identity`, so the consumer declares and the root satisfies |
| `src/contexts/exports/app/command/request.ts` | Three writes, one commit. **Any two without the third is a state nobody has code for** — an operation with no job says *running* forever, a job with no export dereferences nothing. One id for the export and its operation, because two would make every route need a join that exists only because two were minted |
| `src/contexts/exports/app/task/run.ts` | The worker's half, and **in `app/task/` rather than `app/command/` because `M4` is right**: that rule requires an exported command to take an explicit `Subject`, and this has no caller at all — a job hands it an id hours after the request. Giving it a `Subject` it ignores would satisfy the rule by pretending. Streamed, so an export is bounded by the store rather than by memory. **Cancellation is checked at a checkpoint** — every 500 rows and once more before settling, which is what stops a cancellation arriving mid-write from producing a `succeeded` the caller was told would not come. CSV is RFC 4180, because `join(',')` works until a display name has a comma in it. A failure deletes the artifact and **rethrows**, so the queue counts the attempt |
| `src/contexts/exports/app/command/cancel.ts` | Cancel. |
| `src/contexts/exports/app/task/sweep.ts` | The sweep. **The bytes go before the key**: a crash between them leaves a row pointing at nothing, which the next sweep fixes; the other order leaves an orphan the sweep can never find, because the sweep walks rows |
| `src/contexts/exports/app/query/read.ts` | **Authorization at download time**, which is the whole reason a download is a separate route: ownership, existence and expiry are asked *now* rather than when the export was created |
| `src/contexts/exports/infra/memory/index.ts` | The twin. Its `writer` is `undefined`, honestly — there is no handle in memory mode, and handing out a fake one would let a caller believe it had joined something |
| `src/contexts/exports/infra/postgres/index.ts` | Binds all three stores to one handle. Two mechanisms make the transaction hold and either is sufficient, which a breakage pass found and the note records |
| `src/contexts/exports/infra/postgres/schema.ts` | One table, with a partial index on `expires_at where blob_key is not null` — the shape the sweep walks |
| `src/contexts/exports/transport/http/routes.ts` | The three routes five conformance cases point at, plus a download that is deliberately a fourth. `format` is the only required field, because the case sends `{"format":"csv"}` and demanding more would fail it for a reason unrelated to what it tests |
| `src/contexts/exports/transport/http/views.ts` | `result.href` is a **reference**, which is what keeps authorization at download time rather than hours earlier |
| `src/contexts/exports/index.ts` | The context root, and the first that binds three substrate modules — each built for this and none of them knowing about it |
| `src/contexts/exports/index.test.ts` | 20 cases. Four of them are the **first end-to-end exercise of `idempotency`** over a request that does not complete synchronously: a replay that is byte-identical, exactly one export started for a replayed key, a mismatched payload refused, and a key on a poll harmlessly ignored |
| `tests/integration/exports/postgres.test.ts` | The three-writes-one-commit case, which the memory suite cannot fail — its transactor hands out `undefined` for a writer because there is nothing to join |
| `src/contexts/webhooks/domain/ids.ts` | Two typed ids, branded locally — `S7` permits `domain/` exactly one import |
| `src/contexts/webhooks/domain/events.ts` | §2.5's names, plus `WEBHOOK_PREFIX`. **These are events about deliveries, not the events being delivered**, and an endpoint subscribing to them is a loop: a failed delivery publishes a failure, which produces a delivery, which fails. One publish per attempt, growing |
| `src/contexts/webhooks/domain/destination.ts` | Four rules against SSRF — https only, no URL credentials, no loopback or private literal, no fragment — checked when the value becomes **durable** rather than when it is used. **It does not pretend to be sufficient**, and a test says so: a name resolving to a private address defeats all four, and the complete answer pins the resolved address at connect time, which belongs to the dialer. WHATWG normalizes `::ffff:127.0.0.1` to `::ffff:7f00:1`, so the first version's check matched nothing |
| `src/contexts/webhooks/domain/endpoint.ts` | Subscriptions are names with one wildcard suffix and **never a regular expression** — a pattern is data somebody else supplied. Failures are **consecutive**, so one success resets the count: totals would disable a busy endpoint that succeeds 99% of the time before a dead one that never worked. Re-enabling forgives the count, or the owner who just fixed their server gets no runway |
| `src/contexts/webhooks/domain/delivery.ts` | Attempts are **recorded, not counted** — *what did my server say* is unanswerable from a counter. Three numbers because they are three questions: the capped history, the total ever, and the budget for this round. **The last was `attempts.length`**, which capped silently and left a replayed delivery one attempt from exhausting — a replay button that did not replay |
| `src/contexts/webhooks/domain/signature.ts` | `id.timestamp.body`, separated. The **timestamp** lets a receiver refuse a replay; the **id** stops a signature being moved between two events with byte-identical bodies; the separator stops `("a","bc")` and `("ab","c")` signing the same bytes. Whole seconds, because that is what the header carries |
| `src/contexts/webhooks/domain/index.ts` | The domain's root, and the fifth context to state `S7`'s consequence |
| `src/contexts/webhooks/domain.test.ts` | 27 cases with **no ports, no adapters and no HTTP** — which is `S7` demonstrated rather than asserted. Two of them found real defects: the v6-mapped loopback, and the replay budget |
| `src/contexts/webhooks/app/ports.ts` | The first out-ports in this repository to include a **client** rather than only stores, which is why this context exists in the showcase |
| `src/contexts/webhooks/app/subscriber/fanout.ts` | One event in, N deliveries out, **in one transaction**. The body is rendered **once** and stored: a retry must send byte-identical bytes because the signature covers them, so re-rendering at attempt time invalidates every signature in flight the next time the renderer changes |
| `src/contexts/webhooks/app/task/deliver.ts` | Three decisions the substrate cannot make: what to sign, whether a 410 means *stop* rather than *try again*, and what a failure costs the endpoint. **It also enqueues the next attempt** — without that the row said *retrying in a minute* while the queue was empty, and every test that drove the task directly still passed |
| `src/contexts/webhooks/app/command/endpoints.ts` | Ownership is the whole authorization model, and 404 rather than 403 — a 403 turns an id into an oracle for other people's integrations |
| `src/contexts/webhooks/app/query/read.ts` | `ownerId` is set from the subject and is **not** a filter a caller may widen, which is the most common way a list route leaks |
| `src/contexts/webhooks/infra/memory/index.ts` | The twin. Its cursor goes through `decodeCursor` **with the ordering name** — the first version parsed the base64 itself and silently accepted a cursor minted for another ordering, which is conformance case 34's defect reintroduced where the case cannot reach |
| `src/contexts/webhooks/infra/postgres/index.ts` | The fan-out match is in SQL against a GIN index: a fan-out that loaded every endpoint would be one full scan per published event |
| `src/contexts/webhooks/infra/postgres/schema.ts` | Two tables. `events text[]` with a **partial** GIN index on the enabled rows, and a unique index on `(endpoint_id, event_id)` — the bus is at-least-once, and without it a redelivered envelope sends the webhook twice under two different delivery ids |
| `src/contexts/webhooks/infra/secrets.ts` | A secret is **derived**, never stored: there is no column to read. **The signature is keyed by the secret, not the keyring** — the first version was another keyring MAC, which produces a tag the receiver cannot verify, and every test of the sending side would have passed |
| `src/contexts/webhooks/transport/http/routes.ts` | `/v1/webhooks` is unspecified rather than forbidden — §3.5 — so it follows the table's rules instead of inventing new ones. The secret appears in exactly two responses and never in a read. Replay answers **202**, because it enqueues and cannot know whether the webhook was sent |
| `src/contexts/webhooks/transport/http/views.ts` | Field by field, never a spread — `exports` published `contentType` on a snake_case wire by writing one. Neither the payload nor the fingerprint appears |
| `src/contexts/webhooks/index.ts` | The context root, and **the first caller `httpclient`, `breaker` and `retry` have ever had** in this collection. The backoff is a table rather than an exponent, because the numbers a receiver experiences are the specification. Two attempts per delivery, not three: a third immediate attempt costs a receiver a duplicate and buys almost nothing when the delivery is retried in a minute anyway |
| `src/contexts/webhooks/index.test.ts` | 19 cases against a `fetch` the file controls, so a 500 that recovers, a 410 that retires and a host that never answers are all behaviour rather than network. The exhaustion case advances **twelve hours** a round, because the schedule reaches ten — ten minutes stalled at the third attempt and looked exactly like a delivery that stopped retrying |
| `tests/integration/webhooks/postgres.test.ts` | The fan-out's atomicity and the at-least-once guard, neither of which the memory twin can fail. **One case binds its adapter to the root pool and passes only `work.writer`** — the version `exports` did not have, where the adapter is bound to `tx` *and* handed the writer, so deleting the parameter breaks nothing and the test still passes |
| `src/shared/openapi/index.test.ts` | 15 cases, and the ones that matter are about **being diffable**: identical bytes from a reordered input, identical bytes from two runs, and different bytes when a schema changes. `security: []` versus an omitted key, and `z.null()` meaning *no body* rather than *a null body*, are the two shapes a generator gets subtly wrong |
| `tools/openapi.ts` | `make openapi` writes `docs/openapi.json`; `--check` is what `make ci` runs. A deliberate break — one optional field added to a request body — fails the build, which is the difference between this module and decoration. `info.version` is the **contract's**, not the build's: a version moving every commit would make the check always fire, and a check that always fires is one nobody reads |
| `src/contexts/identity/transport/http/routes.ts` | Cases 5–17 as HTTP. Login declares **one** failure status, because case 7 says there is only one answer. `subjectOf` builds the authorization subject from the **owner's roles** with the **key's scopes** attached — never from the scopes instead, which would make a scope a grant and invert case 17 |
| `src/contexts/identity/transport/http/authn.ts` | **Exists because of a defect the end-to-end tests found.** The router resolved the bearer token itself: every route returned the right thing, every repository wrote the right row, and the chain's provenance still said `anonymous:` — so every event this context published named no actor, and `audit` would have recorded *somebody disabled this account* with no answer to *who*. Nothing below the edge could have caught it, because nothing below the edge was wrong. Position 6 now sets the actor and the router reads what it resolved, memoized on the `Exchange` so the session is read once |
| `src/contexts/identity/transport/http/validators.ts` | **`conditional`'s first implementer**, and the shape held. The tag is over the **representation**, not the version: `User.version` would have served, but a version is an *entity* identity and an ETag is a *representation* identity — two variants at the same version must not share one. Hashing the view through the canonical JSON already in the repository is what makes it **strong**, which is what `If-Match` requires |
| `src/contexts/identity/transport/http/views.ts` | Views, not aggregates. A response shape is a published contract; serializing the aggregate ties every refactor to the API and is how a `passwordHash` reaches a JSON body. Nothing here can leak one because nothing here reads one |
| `src/contexts/identity/index.test.ts` | 65 tests, cases 5–17, through the **real chain**. Case 7's timing half is asserted by **counting verifications rather than timing them** — a wall-clock assertion on a memory-hard hash is flaky by construction and gets loosened until it asserts nothing. The first version compared status and body only, and a sweep showed it passing against a login that skipped the hash entirely |
| `tests/integration/identity/postgres.test.ts` | The same contract against a real database, plus eight concurrent signups where exactly one wins, a unit that rolls back data and event together, and a no-password user stored as NULL rather than as an empty string |
| `src/contexts/audit/index.ts` | **The context root, and it imports nothing** — which is the property rather than a constraint. `audit` exists to prove a complete record can be built from events alone, so the day it needs an import is the day an *event* needs a field. Exports the subscription the root registers, the read handler, the declared routes and the migrations. Note: `notes/domain/audit.md` |
| `src/contexts/orgs/index.ts` | **The context root, and the only way in** (`../CONTEXTS.md` §8 step 6). Exports the handler, the declared routes, the migrations, and `membershipsOf` — the half of the cross-context port this context **satisfies**. `STORAGE=memory` when no `db` is passed. Note: `notes/domain/orgs.md` |
| `src/contexts/orgs/domain/ids.ts` | Three typed ids, branded **locally** because `S7` permits `domain/` exactly one import and `brand` is not it |
| `src/contexts/orgs/domain/role.ts` | **A role inside an organization**, and the reason this context is in the constant set. `identity`'s roles say what somebody may do to the *system*; these say what they may do inside **one** organization, and the same person is an owner of theirs and a member of yours. Ordered rather than a permission table, so `atLeast` is an index comparison and a fourth role adds no rows to get wrong |
| `src/contexts/orgs/domain/organization.ts` | Found, rename, archive. Deliberately small: the interesting rules are about the **roster**, and putting them here would be putting a set invariant on one member of the set. **Archived is not deleted** — the memberships, the trail and the slug stay, because releasing the slug lets somebody take the name of an organization whose records still exist |
| `src/contexts/orgs/domain/membership.ts` | A user in an org with a role in that org. **The user id is an opaque string, not an `identity` type** — `S6` forbids the import, and that is the right answer rather than a limitation worked around: this context knows a membership names *somebody* and nothing about what makes somebody exist. `changeRole` does **not** check the roster, because the roster is not this object's to see |
| `src/contexts/orgs/domain/roster.ts` | **The first invariant in this collection that spans a set** — `../CONTEXTS.md` §7.1 notes every v1 invariant was intra-aggregate; this one is not. A pure function over a roster snapshot, called inside the transaction that already read it: on `Membership` it would need every sibling to answer, and on `Organization` every role change would rewrite the whole roster and move the org's version. One function for demote, remove and leave, because they are the same question asked three ways and three checks is how the third misses the case |
| `src/contexts/orgs/domain/invitation.ts` | **The `Challenge` shape, and not the `Challenge` code.** `S7` puts no shared module within reach of a `domain/`, so the aggregate cannot be imported — which is the rule working: two contexts sharing one aggregate would be two contexts with one model. Single-use, TTL'd, keyring-aware, and the **role is inside the MAC**, without which an invitation to be a *member* is byte-identical in effect to one for *owner*. One indistinguishable refusal for every failure, the same rule case 13 fixes for identity's links |
| `src/contexts/orgs/domain/events.ts` | §2.5's names, and a cross-context contract rather than this context's choice — `audit` imports no context and cannot look anything up. `MemberRemoved` and `MemberLeft` are separate because the difference is *who did it*, which is exactly what an audit trail exists to record |
| `src/contexts/orgs/domain/index.ts` | The domain's root, and the second time `S7`'s consequence has been stated: `result` is unreachable here, so value objects throw. Making the same choice twice is what turns a judgement into a convention |
| `src/contexts/orgs/app/ports.ts` | The out-ports, declared by `app/` and injected by the root (`S8`). `roster()` is the one that matters — it is read **inside the transaction about to change it**, which is what makes the last-owner check correct rather than probable |
| `src/contexts/orgs/app/authorize.ts` | **Authorization inside one organization.** Not in `authz`, because a `Subject` carries one role set for the whole request and cannot express *owner here, member there* — encoding org roles into one would need a subject per organization, and the count is not known when the subject is built. A non-member gets **404, not 403**: a 403 confirms the org exists and turns any id into an oracle for which organizations exist |
| `src/contexts/orgs/app/command/organizations.ts` | Found, rename, archive. **The founder becomes its first owner in the same transaction** — an org with no memberships is one nobody can administer, and it satisfies the last-owner invariant vacuously because a roster of zero has nobody to refuse |
| `src/contexts/orgs/app/command/memberships.ts` | The roster commands, each reading the roster inside the transaction it is about to change. **Only an owner makes an owner**, or an admin who could promote themselves is already an owner and the ladder has no rungs |
| `src/contexts/orgs/app/command/invitations.ts` | Invite, accept, revoke — mechanics from `shared/token`, aggregate from this context. **The mail is sent after the transaction commits**: a mail for a row that rolled back is an invitation nobody can accept, and a committed row whose mail failed is revocable and visible. Accepting does not compare the invited address to the caller's, because `S6` means this context cannot see one — possession of the secret is the proof |
| `src/contexts/orgs/app/query/read.ts` | Reads, every one scoped by membership rather than an account role. The caller's own role travels with the org view, so a client rendering it does not need a second round trip and a second chance to disagree |
| `src/contexts/orgs/app/query/roles.ts` | **The satisfying half of the cross-context port.** No `Subject` and no authorization: this is not a use case a caller invokes, it is the data `identity` needs in order to *build* a subject, and a check here would be circular by construction |
| `src/contexts/orgs/app/index.ts` | The app layer's root |
| `src/contexts/orgs/infra/memory/index.ts` | `STORAGE=memory` — a real mode, invariant `I1`. The transaction is a snapshot-and-swap, which is enough for the one property the contract asks: a `within` that throws leaves the store as it found it. It does not pretend to give concurrency, which is the PostgreSQL adapter's to prove |
| `src/contexts/orgs/infra/postgres/index.ts` | Every write on `(id, version)`. `roster()` reads `for update`, which locks the rows the last-owner check reasons about — without it two concurrent demotions of two different owners each see two and the organization ends with none. `nolint:tenant` throughout, with the reason stated per statement: an organization **is** the scope here, and a tenant column beside it would be a second, weaker fence |
| `src/contexts/orgs/infra/postgres/schema.ts` | Three tables. **No foreign key from memberships to `identity_users`** — `S6` makes the contexts islands, and a constraint across them is the same coupling written in SQL: an identity migration would become an orgs migration, and a user delete a foreign-key error in another context |
| `src/contexts/orgs/transport/http/routes.ts` | Organizations as a resource, on the shared registry. `../CONFORMANCE.md` §3.5's table does not name these — they are reported to the collection as **constant-set routes the table is missing** rather than quietly kept. The accept route takes its token **in the body and not the URL**, and is not under `/v1/orgs/{id}` because the acceptor does not know the id: the token does |
| `src/contexts/orgs/transport/http/validators.ts` | **The second implementer of `conditional`'s `Validators`, and it found the gap the first could not.** The module takes **one** function, and until this context existed there was exactly one implementer, so the root passed it straight through — which worked and hid the assumption. The routes here declared `validated: true`, `S11` correctly made them declare 304 and 412, and **nothing supplied a tag**, so neither status could ever be produced: a declaration nothing implements is the same failure as a route nothing mounts, and it looks exactly like working software. The tag is computed **per caller**, because the caller's own role is in the view and two members with different roles see two different representations |
| `src/contexts/orgs/transport/http/views.ts` | Views, not aggregates. **No secret and no fingerprint appears in any of them** — nothing here reads one, so nothing here can leak one |
| `src/contexts/orgs/index.test.ts` | 18 cases. The ones that matter are §4's: one person with two roles in two organizations, an action refused in one org that the same caller may take in another, and all three halves of the last-owner rule — including the **second** demotion after the first was allowed, which is the case a per-handler check gets wrong |
| `tests/smoke/orgs-disabled.test.ts` | **`ORGS_ENABLED=false` boots and serves.** The only mechanical test that the cross-context port is real: everything else about the seam is visible by inspection and therefore satisfiable in prose, including of a port whose absence breaks the process |
| `src/contexts/audit/domain/record.ts` | Append-only: no mutation, no `save`, because a record that can be edited is not an audit record. `subject` is a bare id because nothing here could resolve it into a name. `toWire` is **snake case with absent fields omitted** — case 38a, where a `null` and an omission are different documents under RFC 8785 and a language emitting one where another omits produces a different digest for the same fact |
| `src/contexts/audit/domain/query.ts` | The six filters §3 names, as a value object, so the **scope** below can be applied to the same structure. A prefix is validated against a shape before it reaches a `like`, because `%` in one turns a filter into a scan of everything. `Scope` is `all`, `own` or `none`, and `own` means **actor *or* subject** — two different sets, and the second is the one that matters: being disabled by an administrator is a record where somebody else acted |
| `src/contexts/audit/domain/index.ts` | The domain's root. Small, and small is the point: an audit log is a set of facts with no relationships between them, so there are no invariants spanning records |
| `src/contexts/audit/app/ports.ts` | Two methods, and **no `save` or `delete`** — their absence is the design, since a port that could edit a record is one somebody eventually calls. `append` returns whether a row was added, which is what makes case 36's idempotency assertable rather than assumed |
| `src/contexts/audit/app/subscriber/record.ts` | **The first subscriber in this repository.** Derives its provenance with `provenanceFor` and **never mints** — case 38, and a subscriber calling `forJob` would break the chain at exactly the point `audit` exists to record, invisibly, with every field populated and only the join gone. Two fields differ deliberately: `correlationId` is the derived one (the originating request's) and `requestId` is the **source's**, because recording is a new unit of work and a reader joins the record against an access log. The actor comes off the envelope and the subject off the payload, never one from the other |
| `src/contexts/audit/app/query/search.ts` | Case 37, and **where `authz` crosses a context boundary for the first time**: a `Subject` built from identity's authenticated caller authorizes a read in a context that has never heard of identity. Uses `reach` rather than `allow`, because the caller has no resource yet — it is about to build a query. `audit` never names `admin` or `auditor`; which roles reach how far is the root's policy |
| `src/contexts/audit/app/ports.contract.ts` | One suite, both adapters. The cases are where an adapter can be plausibly wrong: the idempotency key being the **event's** id rather than the record's, the dot as the prefix boundary, and the scope **ANDing** with a filter rather than being replaced by it — a caller narrowing to somebody else's id must still see nothing |
| `src/contexts/audit/infra/memory/index.ts` | `STORAGE=memory`, invariant `I1`. The dedupe is a `Set` of event ids, which is the same guarantee the unique index gives — for one process, which is exactly what it claims |
| `src/contexts/audit/infra/postgres/schema.ts` | **`nolint:tenant`, and `audit` is the one context where `M3` inverts.** The table records events from every context including ones with no tenant at all — a boot, a migration, a job — and `where tenant = $1` would silently hide exactly the system actions an auditor is looking for. What protects a caller's records is `Scope`, which is per **caller** and therefore stricter. The prefix index carries **`text_pattern_ops`**: the default opclass uses the database's collation and a `like 'identity.%'` will not use an index built with it, which turns every prefix search into a scan of the biggest table here — silently, and only once there is data |
| `src/contexts/audit/infra/postgres/index.ts` | `on conflict (event_id) do nothing` — case 36, and `rowCount` is what tells the caller which happened. The scope goes into the `where` **first and ANDed**, never merged into the filter. `escape '\'` is written out because the default is backslash on most builds and is not guaranteed, and a pattern whose escapes are inert matches more than it says |
| `src/contexts/audit/transport/http/routes.ts` | **The second copy of the route-registration shape**, because `S6` makes identity's unreachable — §8 names *after the third context* as when that stops being acceptable. **`audit` does no authentication**: it takes a `caller` function the root supplies from identity's bearer auth and never sees a token, which is how it knows who is asking without importing the context that knows what a token is |
| `src/contexts/audit/infra/memory/index.test.ts` | The memory adapter against the shared contract |
| `tests/smoke/audit-records-identity.test.ts` | 16 tests, and **the first in this repository wiring two contexts together** — through the bus, never through an import. Cases 34–38 end to end: identity's events recorded without identity being imported, an actor that is *not* the subject, a redelivery adding no row, a client-supplied correlation id surviving to the record, and a caller unable to widen their reach with a filter. **It lives here because `S6` put it here**: written first inside `audit`, it tripped *contexts never import each other* — and the rule was right, because a test that wires two contexts is a composition-root test rather than a context's own. `S6` exempts no test file, which turns out to be the correct strictness rather than an oversight: the exemption would have let a context's own suite reach for a peer. Rung 0, like the other smoke test |
| `tests/smoke/chain-positions.test.ts` | **A declared chain position is mounted or announced — nothing in between.** A sibling shipped `ratelimit` finished and passing its own suite with position 7 still a pass-through: nothing was red, because the module's tests called the module and the root's tests asserted a request succeeded, which it does faster with no limiter in it. The skip-list half of this is hand-kept and says so; the half with teeth is the probe, which reads `RateLimit-*` off a 401 and therefore fails the moment `wire` stops handing position 7 anything |
| `tests/smoke/journal-scrubbing.test.ts` | **The journal scrubs both directions**, which it did not. Scrubbing was added when a bearer token turned up in a *response*, and the fix looked complete because the thing it was written for stopped appearing — so every password the journey typed went into the artifact in the clear, in a file whose whole purpose is being handed to somebody. The values were fixtures and nothing leaked; the **mechanism** is what ships. Two fixes: field names are harvested automatically through `redact`'s own vocabulary, so nothing has to be remembered, and a non-JSON response body is scrubbed at **render** rather than only at capture, since the alias table only grows. Aliases rather than redactions, because a transcript full of `[redacted]` is documentation and one that keeps its shape is a tool |
| `tests/integration/audit/postgres.test.ts` | 19 tests against a real database. Twelve concurrent appends of one event id where exactly one wins — the memory `Set` proves that for one process, and four replicas draining the same outbox is what the unique index is for. Plus a planner check that the prefix index is actually usable, which is the failure that would otherwise appear only under load |
| `tests/integration/orgs/postgres.test.ts` | **The last-owner race, which the memory twin cannot show.** JavaScript runs each memory callback to completion, so there is no interleaving in one process; two connections demoting two *different* owners each read a roster with two and each conclude they may proceed. `roster()` reads `for update`, so the second blocks until the first commits and then reads a roster with one — one demotion succeeds, one is refused, and the organization keeps an owner. Plus the three uniqueness and optimistic-write cases that are only real against an index |
| `src/shared/conditional/index.ts` | **L4, position 9 beside `idempotency`**, and the last phase-1 module in the layer. **Ships in two halves and only one exists**: RFC 9110 fixes the grammar, the comparisons and the evaluation order, so those are here and none of it is a guess; how a handler supplies its validator is an interface with **no implementer**, because no aggregate exists to write one. The same line the collection drew for the canonical-JSON fixtures. Note: `notes/patterns/conditional.md` |
| `src/shared/conditional/etag.ts` | §8.8.3 grammar and §8.8.3.2's **two comparison functions, which are not interchangeable** — `If-Match` is strong, `If-None-Match` is weak, and writing one for both passes casual testing because the two agree whenever no weak tag is involved. **The derived tag is strong, and forced rather than chosen**: a weak validator never matches under strong comparison, so a `W/"v42"` tag makes case 29 return 412 *permanently* — passing for the wrong reason, which is worse than failing, because nothing ever surfaces it. Strong needs a canonical serialization, and this repository has one already from the event-digest work; that is luck rather than planning and the note says so. The variant is joined with U+001F so a JSON and a CSV representation cannot share a tag. List parsing is **quote-aware**, because `%x2C` is a legal `etagc` and `split(',')` turns `"a,b"` into two malformed entries — a bug invisible here, since the tags this repo emits are hex digests |
| `src/shared/conditional/preconditions.ts` | §13.2.2, and **the order is normative rather than a preference** — a request carrying two preconditions has exactly one defined outcome. Two subtleties tested by name: `If-Unmodified-Since` is consulted only when `If-Match` is *absent*, not merely when it passed; and `If-Modified-Since` only when `If-None-Match` is absent. A failed `If-None-Match` is **304 on GET/HEAD and 412 on a mutating method** — the branch a browser never exercises, so it is asserted across PUT, POST, PATCH and DELETE. Dates are **ignored when malformed** (§13.1.3, so a broken proxy cannot 412 every conditional GET) and compared at **second** precision, because HTTP-date has no more and millisecond comparison makes every sub-second write look newer than any header could express. `If-Range` takes no weak tag ever: a partial response is spliced into a cached one, and *semantically equivalent* would produce a document that never existed |
| `src/shared/conditional/validators.ts` | **The half that waits, and it is deliberately empty.** One type, no implementation — the same shape `httpx` used when it left position 5's budget reachable before `deadline` existed. An interface with no implementer is the point: it makes the first aggregate's obligation visible when it is written rather than discovered afterwards. It takes the whole `Exchange` rather than a resource id, because a tag identifies a **representation** and an implementer needs `Accept` to know which one — an id-only signature would make the variant unrepresentable and lock the mistake in. Two decisions left to the first aggregate: whether collection endpoints carry tags, and whether the tag is computed or stored |
| `src/shared/conditional/middleware.ts` | Runs **inside** `idempotency` rather than outside, and the reason is stated where somebody would otherwise reorder it: a replay must return the stored response bit for bit, and re-evaluating preconditions against state that has moved would turn a replay into a 412. The cost — a fresh request spends a key before its preconditions are checked — is the cheaper one, since a spent key is recoverable and a replay answering 412 is not. The 412 is **thrown**, so it goes through the same mapper as every other error; the 304 carries the validator and no body, because §15.4.5 says a 304 sends what a 200 would have and a cache with no tag has nothing to refresh from |
| `src/shared/conditional/index.test.ts` | 65 tests. The §8.8.3.2 comparison table asserted as a **disagreement** rather than a side effect, including a weak tag failing to match *itself* under strong comparison; both branches of a failed `If-None-Match`; the two precedence subtleties; second-precision dates and ignored malformed ones; and end to end through the real chain — case 29's 412 as an RFC 9457 body with a request id, case 30's `ETag` on a GET and 304 with no body, and a derived tag emitted, echoed back and matched |
| `src/shared/httpx/index.ts` | **L4, and the first of the layer.** Owns the server port, safe timeouts, the middleware chain written once, and RFC 9457 problem mapping — **not** routing tables, not handlers, not any context. The order is `../MODULES.md` §5's rather than this repository's, because eight repos each inventing a plausible one produces eight different answers to *does a 429 carry a request id*. Note: `notes/patterns/httpx.md` |
| `src/shared/httpx/problem.ts` | Invariant `I7`'s *mapped to transport codes only in transport*, as a file. The whole `Kind` → status table is conformance case 3 and lives here and nowhere else — the moment a second file knows what 409 is, there are two answers to what a conflict is. **Eleven kinds, eleven statuses, total by construction**: `Record<Kind, number>` means adding a kind without a status does not compile, which is why collection decision 0010 chose a new `Kind` over a status carried on the error. A **cancellation is never rendered** — 499 is for the log, not the wire, and there is nobody on the socket to send a body to. A **5xx `detail` is generic** because an internal message names a table, a host or a driver and the caller can do nothing with it; a 4xx `detail` is the message verbatim. `Unavailable` is generalised alongside `Internal`, which is case 4: `httpclient` refuses to put an upstream body in a message, and this is the second half of that promise. Validation returns **every** problem at once in an `errors` map keyed by field path |
| `src/shared/httpx/chain.ts` | The order, as executable contract. **Recover sits *inside* the problem mapper**: caught outermost it would have to render its own response, and then there are two places that build an error body — inside, a panic and a returned error are indistinguishable to the client. **Ratelimit sits *after* authn**, because case 40 wants per-caller limits and a limiter running first can only key on the peer address, which is one NAT from useless; the auth check spent on a throttled caller is noted rather than the order reversed. Positions 5, 7 and 9 are **named and empty**, declared as options rather than described in a comment. This module is the **one and only caller of adoption**, and the allowlist type carries no request id, actor or tenant, so the bypass cannot be written |
| `src/shared/edge/index.ts` | **The floor of L4** (`../ARCHITECTURE.md` §L4, which names it), and the same shape as L0's vocabulary one layer up — it carries the layer's own name because it is the layer's vocabulary, the relationship `errors` has to L0. `Request`, `Response`, `Exchange`, `Handler`, `Middleware`, `Reporter` — the types every L4 module needs to be written at all. `httpx` **assembles** the chain; `idempotency`, `ratelimit` and `conditional` are **positions in** it. Left inside `httpx`, each of them imports a peer: permitted by `S1` and flagged in review, which turns a flag meant to catch something into one that fires on every module in the layer and stops being read. **Framework-neutral because this repository ships two servers behind one port** — a stronger reason than the general one, and the reason the adapter suite can run every case through both and compare the answers to each other. `Exchange.remaining()` is `../RESILIENCE.md` §4's *leave the budget reachable*; **the committed-tracking response writer is deliberately absent** and says so, because `Response.body` is a `string` so nothing behind this chain streams and the late-error rule is satisfied vacuously — which is a real answer only while the premise holds. Note: `notes/patterns/edge.md` |
| `src/shared/httpx/server.ts` | The server port, and four timeouts with **values rather than defaults-by-omission** — Node's have changed between majors, and relying on one is relying on a number nobody wrote down. Distinct from position 5's budget: these protect the process from a peer that will not finish speaking, the budget bounds the work a handler may do once it has. `stop()` drains rather than kills, which is what makes it a `lifecycle` step |
| `src/shared/httpx/guard.ts` | **Exists because the documented lever did not work.** `server.headersTimeout` is Node's answer to slowloris and setting it is not enough: on Node 22.21 a peer that stalled mid-headers was still connected ten seconds later in every configuration measured, including `connectionsCheckingInterval` at 100ms. A socket-level inactivity timer does fire, so one is armed on connect and **released once the headers arrive** — the release is the design, since armed for the connection's life it is a read-header timeout wearing the wrong name and a handler that thinks for half a second is cut off mid-answer. Installed by both adapters on the same `http.Server` |
| `src/shared/httpx/node.ts` | The `node:http` adapter. Bodies read through a cap, because `Content-Length` is a claim and not a limit — the same reason `httpclient` caps a response. `stop()` closes idle connections and waits for in-flight ones |
| `src/shared/httpx/fastify.ts` | The Fastify adapter, and the reason the chain is framework-neutral. Fastify owns routing, schema and the socket; the chain owns the cross-cutting order — wrapping Fastify to look like `node:http` would be the failure, and so would re-implementing routing to avoid using it. `logger: false` and `requestIdHeader: false` because position 2 and position 1 already own those, and a second log line with a different field set is what conformance §4.13 exists to prevent. `connectionTimeout` is deliberately **0**: it is a whole-socket inactivity timer, so leaving it on would disconnect a peer waiting on a working handler through one server and not the other |
| `src/shared/httpx/index.test.ts` | 28 tests on the chain itself. The order matches the published positions; a request id is on a 200, a 429 and a panic alike; a panic and a returned `internal` produce byte-identical bodies bar the `instance`; every validation problem arrives at once; an upstream body never reaches the response; correlation, causation and traceparent are adopted while the request id is **minted and echoed as minted**; the log line carries `err_kind` and the header that carried it is stripped. Found a real defect: recover was wrapping typed errors, so a 404 reached the client as `the request could not be handled: no such thing` |
| `src/shared/httpx/server.test.ts` | 12 tests on real sockets, **every case run through both servers and the two answers compared to each other** rather than to a transcript — a transcript can be edited to match whichever adapter drifted, the other adapter cannot. `instance` and `x-request-id` are lifted out and asserted by shape, since two servers mint two ids and leaving them in makes every comparison fail vacuously. The slowloris case is a raw socket that sends a request line and stops, and it is paired with a 600ms handler that must still be answered — which is the assertion that keeps the guard a guard |

### Notes — language

Module notes are cited from each module's row above. These are not tied to a
module: `../TEMPLATE-CLAUDE.md` §5.2 gives `notes/language/` to *syntax, idioms,
standard library, type-system tricks*, and in a repository whose thesis is that
**the language is the only variable**, that is the category the comparison
actually turns on.

| File | Description |
| --- | --- |
| `notes/language/type-system.md` | Where the type system does architectural work: branded types for nominal typing in a structural language, closed enums without `enum`, a type and a value under one name, compile-time assertions (the `Transactor` drift check), and `#private` as a security control rather than a style — `private` is erased and reachable, `#` is not |
| `notes/language/strictness.md` | Four `tsconfig` flags beyond `strict` and the code each one forced. `exactOptionalPropertyTypes` is the interesting one: the `...(x === undefined ? {} : { x })` spread it demands **is** the collection's omit-absent-never-null wire rule, so the compiler enforces cross-language parity for free |
| `notes/language/stringification.md` | The four independent paths from a value to text, depended on in opposite directions: `redact` must close all four or a secret leaks through the one that was missed, and `digest` gets RFC 8785 almost free because the RFC defers to ECMAScript for numbers, string escaping and key order. `.sort()` is correct only because its default comparator is UTF-16 code units |
| `notes/language/numbers.md` | JavaScript has **one** number type plus `BigInt`, and no `int64` — the single largest place TypeScript and Go are genuinely not interchangeable. Where 2^53 is actually touched here: advisory lock keys (`BigInt` end to end), Postgres `bigint` arriving as a **string** on purpose, and RFC 8785's `max_safe` vector being the last value all three languages agree on without ceremony. Also `Date` being milliseconds where `timestamptz` is microseconds — a difference to know rather than a bug to fix |
| `notes/language/runtime.md` | ESM and Node: `.js` specifiers on TypeScript imports, `AsyncLocalStorage` and the one place it is deliberately not used, the event loop not staying open for a signal handler (exit 13, twice), and why draining microtasks takes `setImmediate` rather than one `await` |
| `notes/language/tooling.md` | Two TypeScript compilers side by side and why neither half could be given up; pnpm's `.pnpm/<pkg>@<version>/` layout and how it made every `S10` rule inert for installed packages; vitest projects and `globalSetup` for anything decided once per run rather than once per worker |

### Rules

| File | Description |
| --- | --- |
| `tests/rules/encoding.test.ts` | Every source file is **text, not data**. Not a numbered rule — `../ENFORCEMENT.md` has no id for it — but it earned a permanent test: `src/shared/digest/index.test.ts` held a raw `0x00`, `0x0f` and `0x1f`, which TypeScript parses and vitest runs happily while `file(1)` reports `data` and **grep skips the file entirely**. The cross-language fixture wiring inside it was therefore invisible to every text tool for four rounds of being told it was missing. No suite here could have caught it, because the suite is what was passing |
| `tests/rules/arch.test.ts` | Proves every rule in `.dependency-cruiser.cjs` actually fires. On an empty repo the cruise passes vacuously, and a rule that has never fired is a rule nobody has tested — so each one also gets a tree built to trip it. Three tests carry the weight: a clean tree reports nothing, each fixture trips exactly one named rule, and no rule ships without a fixture |
| `tests/rules/routes.test.ts` | **S11**, read off the real route tables rather than parsed out of source — routes are values, and a rule that can build the thing it governs should. Found 29 undeclared statuses across 17 routes on its first run, which is the number `openapi` would otherwise have published as the contract. The tables are built with stub dependencies because nothing here calls a handler; the declaration is what is under test |
| `tests/smoke/main.test.ts` | The in-process composition smoke test — the file rule `S9` exempts by name. A unit test proves a module works; this proves the graph can be built at all. Runs at rung 0: no Docker, no network, no build |
| `tests/rules/semantic-rules.ts` | Rules about what the code does rather than what it imports, so they parse the syntax tree with ts-morph rather than the import graph. `M2`: no module reads `Date.now`, `performance.now` or zero-argument `new Date()` — `new Date('2026-01-01…')` is allowed, because naming an instant is not reading one. `I5`: only `random` touches entropy. `M13`: `breaker`, `ratelimit`, `retry`, `deadline` and `timers` measure durations on the monotonic reading, never on `now()`. **`I5` is named for the invariant, not for an `M` number** — `../ENFORCEMENT.md` covers the clock half of I5 and not the randomness half, and minting a local rule id would put this repo's rule set out of step with its siblings. It renames if the collection gives it one |
| `tests/rules/semantic.test.ts` | Runs the M rules against this repository and against a fixture per rule, including a `clean/` tree that exercises both exemptions — the `clock` module, and a literal date |
| `tests/rules/docs-rules.ts` | The N, D and R rules as functions over a repository root, so the same checks run against this repo and against fixture roots. Its header records the rules deliberately not implemented yet and why. **`N4` resolves a link against `notes/**`, then `docs/decisions/`, then the collection's `decisions/`** — by *existence*, so a citation of a renamed decision still fails, which is the property the rule is for. A bare four-digit link never resolves: local ADRs and collection decisions share one numbering sequence, so `[[0009]]` is ambiguous to a reader and silently resolvable to the wrong document by whichever directory a checker reaches first. The missing-directory failure attaches to the **link**, not the tree, so publishing standalone cannot switch the rule off while a repo citing no decisions is not failed for having none. **`N6` stays scoped to local ADRs** — a collection decision's status is not this repo's to maintain, and going red on an edit it did not make for a document it cannot fix would be worse than the gap |
| `tests/rules/docs.test.ts` | Runs those rules against this repository — which must satisfy every one — and against a fixture per rule. The repository test is the one that fires when a document drifts, which is what N5 and R3 exist for |
| `tests/rules/fixtures/` | One directory per rule. `arch/` holds minimal `src/` trees that each violate one import rule; `docs/` holds minimal repository roots that each violate one document rule. Both carry a `clean/` control that violates none. Excluded from `tsc`, ESLint and `make arch` — they are broken on purpose |

### Rules not yet enforced

**A rule deferred needs a reason, the same way a module deferred needs a
trigger.** Sixteen of the N, D and R rules are enforced today, alongside all
nine S rules; these are not, and none of them is an oversight.

| Rule | Why not yet | Lands |
| --- | --- | --- |
| **N8** · `notes/INDEX.md` is current | There is no generator to diff against | Phase 9, with the generator |
| **R1** · README has the standard sections | Installation, Quick Start, Usage, Configuration and Requirements would be fiction — nothing runs yet | When `make dev` serves a request |
| **R2** · ARCHITECTURE names every module with its layer | The document says so itself: *"to be completed as the repo is built"* | Progressively, as modules land |
| **An `M` id for `I5`** | `../ENFORCEMENT.md` has M2 for the clock half of invariant I5 and nothing for randomness or identifiers. The detection exists here under the invariant's own id rather than a locally-minted number | When the collection assigns one |
| **M1, M3–M11** | Each needs the thing it governs. M1 needs a port with two adapters, M3 a repository, M4 a use case, M5 an event envelope — a rule written first would have nothing to parse and no fixture worth writing | With the module that defines it |
| **D3** · Accepted ADRs are immutable | Needs history, not the working tree. `git log` knows; a file-parsing test cannot | Never — a review habit |
| **D6** · When an ADR is required | A judgement about a decision, not a property of a file | Never — a review habit |
| **R8** · `STATUS.md` is current | Gitignored, so CI cannot see it. `../ENFORCEMENT.md` states this limit rather than pretending otherwise | Never — a session habit |
