# The toolchain pin is enforced, not documented. Installing on the wrong Node
# fails here rather than three rungs up the ladder.
engine-strict=true

# A dependency resolves only if something declares it. Phantom dependencies are
# exactly the class of import the architecture rules exist to catch.
resolution-mode=highest
strict-peer-dependencies=true
