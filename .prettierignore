node_modules/
dist/
coverage/
pnpm-lock.yaml

# Markdown is hand-written and hand-wrapped: prettier normalizes *emphasis* to
# _emphasis_ and reflows ASCII tables, both of which diverge this repo from the
# collection's documents. What matters about these files is enforced by the
# N and R rules instead — see ../ENFORCEMENT.md.
*.md

# Generated by `make openapi`, and formatted by the generator.
#
# Two formatters over one file is one file with two authoritative spellings:
# prettier would reformat it, `make openapi-check` would then see a diff, and
# `make ci` would fail on a change nobody made. The generator's `render` owns
# the bytes precisely so the drift check means something.
docs/openapi.json
